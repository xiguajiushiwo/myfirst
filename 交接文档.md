# 云小圈 AI 硬件质检系统 — 换机部署交接文档

> 面向：把本项目从当前机器迁到另一台电脑并跑起来。
> 编写日期：2026-07-30。文档中的数据均在当前机器实测得到。

---

## 一、这个项目是什么

内存条（DDR5 RDIMM）**外观 + 日期码**自动质检系统。一个托盘并排放 4 根，上下两台工业相机同时拍正反面，然后：

1. **PaddleOCR** 按模板固定框逐颗读日期码（存储颗粒 3 位 YWW、PCB/主控 4 位 YYWW）
2. **Qwen-VL 大模型** 做外观质检（元器件损坏/发黑、金手指、芯片打磨痕迹）
3. **本地 zbar** 解二维码拿 SN
4. 逐根独立判定 → 拆成 4 条记录入库（MySQL）

关键业务规则（**不可改**）：
- **逐颗看清日期，绝不用多数表决吸收离群值** —— 个别颗粒可能被偷换，必须能查出来
- **SN 只认二维码精确解码**，解不出就留空待人工，绝不用大模型猜
- 逐根独立判定，不跨根比较

---

## 二、当前机器的运行环境（迁移目标要对齐）

| 项目 | 当前值 | 换机注意 |
|---|---|---|
| 操作系统 | Windows 11 Pro | 海康 SDK 只有 Windows 版，**必须 Windows** |
| Python | 3.12.13 | 要求 `>=3.12` |
| 包管理 | **uv**（不是 pip） | 新机器需先装 uv |
| 显卡 | RTX 3080 20GB | 见下方"显卡要求" |
| CUDA | 13.1（驱动 591.86） | 装 paddlepaddle-gpu 要对应 |
| 数据库 | MySQL（本机 127.0.0.1:3306） | 需在新机器装 MySQL 并迁数据 |
| 相机 SDK | MVS，装在 `E:\MVS\Development` | 需重新安装 |

### 显卡要求（实测占用很小）

```
空闲基线    1072 MiB / 20480 MiB
运行峰值    1713 MiB / 20480 MiB
质检净占用   641 MiB  = 3.1% 显存
GPU 利用率   峰值 56%（未打满，瓶颈在逐框调度不在算力）
```

**只吃 641MB 显存**，所以新机器不需要 3080 这么强，**6GB 显存的卡就够**。没有 N 卡也能跑：把 `run.bat` 里 `OCR_DEVICE` 改成 `cpu`，代价是 OCR 从约 5 秒涨到几十秒。

---

## 三、迁移步骤

### 步骤 0：先做这件事 —— 本项目没有 git 远端

当前仓库**没有配置远端**，且有 50 个未提交文件。所以**不能靠 git clone 迁移**，必须整目录拷贝。

```bash
# 在当前机器：确认没有远端
git remote -v      # 输出为空
```

**推荐做法**：把整个 `E:\AIpaddle` 目录拷到移动硬盘/网盘，再复制到新机器。

拷贝时**可以排除**这些（体积大且能重新生成）：

| 目录 | 大小 | 说明 |
|---|---|---|
| `.venv/` | 数 GB | 虚拟环境，新机器重建 |
| `outputs/` | 1.4 GB | 标注框图，运行时重新生成 |
| `uploads/` | 462 MB | 历史拍摄原图，按需保留 |
| `logs/` | 5.3 MB | 日志 |

**必须带上**的：
- `app/`、`web/`、`tools/`、`tests/`、`config/`、`scripts/`
- `models/`（**166MB，OCR 模型，见步骤 4**）
- `.env`（**含密钥，注意保密传输**）
- `pyproject.toml`、`requirements-full.txt`、`run.bat`、`conftest.py`
- `app/recognition/templates/`（**模板 JSON，丢了要重建，很费时**）

### 步骤 1：装 Python 3.12 和 uv

```bash
# 装 uv（官方安装脚本）
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# 装 Python 3.12
uv python install 3.12
```

### 步骤 2：重建虚拟环境和依赖

```bash
cd E:\AIpaddle
uv venv --python 3.12
```

**注意**：`pyproject.toml` 里的 `dependencies` 是**空的**（历史遗留），不能靠它装依赖。用我导出的完整清单：

```bash
uv pip install -r requirements-full.txt -i https://mirrors.aliyun.com/pypi/simple/
```

如果 `paddlepaddle-gpu` 装不上（阿里云镜像可能没有 GPU 版），单独从官方源装：

```bash
uv pip install paddlepaddle-gpu==3.3.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
```

> CUDA 版本要与新机器驱动匹配。当前机器 CUDA 13.1，用的 cu126 包（向下兼容）。
> 新机器 CUDA 版本用 `nvidia-smi` 查，然后去 [PaddlePaddle 官网](https://www.paddlepaddle.org.cn/install/quick) 选对应安装命令。

核心依赖版本（实测可用组合）：

```
paddleocr==3.7.0            paddlepaddle-gpu==3.3.1     paddlex==3.7.2
fastapi==0.138.1            uvicorn==0.49.0             numpy==2.3.5
opencv-contrib-python==4.10.0.84                        pillow==12.2.0
opencv-python-headless==4.13.0.92                       pymysql
python-multipart==0.0.32    openpyxl==3.1.5             requests==2.34.2
```

验证 GPU 可用：

```bash
.venv\Scripts\python.exe -c "import paddle; print(paddle.is_compiled_with_cuda(), paddle.get_device())"
# 期望输出：True gpu:0
```

### 步骤 3：装 MySQL 并迁数据

新机器装 MySQL（8.x），然后从当前机器导出数据。

当前数据量（实测）：

| 表 | 行数 | 说明 |
|---|---|---|
| `inspection_records` | 138 | **质检记录，核心业务数据** |
| `batches` | 398 | 采购订单/批次 |
| `audit_log` | 101 | 操作审计 |
| `sessions` | 70 | 登录会话（可不迁，会重建） |
| `operators` | 3 | 质检员 |
| `users` | 2 | 登录账号 |

导出（当前机器）：

```bash
mysqldump -u root -p yunxiaoquan_qc > yxq_backup.sql
```

导入（新机器）：

```bash
mysql -u root -p -e "CREATE DATABASE yunxiaoquan_qc CHARACTER SET utf8mb4"
mysql -u root -p yunxiaoquan_qc < yxq_backup.sql
```

> 如果不需要历史数据，可以跳过导入 —— 服务启动时会自动建库建表（见 `app/server.py`）。

### 步骤 4：OCR 模型（已放进项目，无需下载）

模型已经放在项目内 `models/` 目录，**随项目拷贝即可，新机器不用联网下载**：

```
models/PP-OCRv5_server_det    85 MB   文字检测
models/PP-OCRv5_server_rec    82 MB   文字识别
```

实现见 [app/recognition/ocr_engine.py](app/recognition/ocr_engine.py)：`_model_dir()` 检查项目内目录存在且含 inference 文件就用它，否则回退到 PaddleOCR 默认缓存（`~/.paddlex`，会联网下载）。也可用环境变量 `OCR_MODELS_DIR` 指到别处。

> 如果拷贝时漏了 `models/`，首次运行会自动从网上下载到 `C:\Users\<用户>\.paddlex`，需要联网且较慢。

### 步骤 5：装海康相机 SDK（MVS）

1. 从[海康机器人官网](https://www.hikrobotics.com/)下载 **MVS** 客户端（Windows 版）
2. 安装（当前机器装在 `E:\MVS`）
3. 安装程序会自动设置环境变量 `MVCAM_COMMON_RUNENV`

代码里的路径（[app/cameras/hik_sdk.py:35](app/cameras/hik_sdk.py#L35)）：

```python
_RUNENV = os.environ.get("MVCAM_COMMON_RUNENV", r"E:\MVS\Development")
_MVIMPORT = os.path.join(_RUNENV, "Samples", "Python", "MvImport")
```

**如果新机器装在别的盘**（比如 `C:\MVS`），环境变量会自动指对，不用改代码。装完**重启一次**让环境变量生效。

### 步骤 6：改 .env 配置

`.env` 里有需要按新环境调整的项。**含密钥，注意保密**。

必须确认/修改的：

```ini
# 数据库（新机器的 MySQL）
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=<新机器的密码>
MYSQL_DB=yunxiaoquan_qc

# 大模型（当前 key 曾欠费停用过，迁移前确认账号可用）
DASHSCOPE_API_KEY=<阿里云百炼 API Key>
QWEN_VL_MODEL=<模型名>

# 相机序列号（跟着相机走，换相机才需要改）
HIK_FRONT_SN=DB1224590        # 上相机·正面
HIK_BACK_SN=DB1623157         # 下相机·反面
HIK_FRONT_ORIENT=none
HIK_BACK_ORIENT=rot180        # 反面需 180° 翻正，见下方"重要"

# 登录账号
ADMIN_USER=admin
ADMIN_PASSWORD=<管理员密码>
```

> **重要 — 不要随意改 `HIK_BACK_ORIENT=rot180`**：这个 180° 旋转既让反面文字转正可读，又反转左右，使正/反两图的槽位都按同一"物理左→右"排序，从而**正面第 i 槽 = 反面第 i 槽**。改成 `none`/`flipv` 会导致正反槽位左右镜像错配，第 1 根的正面配上第 4 根的反面。见 [app/services.py](app/services.py) 里的对齐说明。

静止即拍的触发阈值（**换机/换光照后必须重标**，见第五节）：

```ini
AUTO_MOTION_THR=4.0
AUTO_STILL_THR=2.6
```

其余配置项（相机曝光/增益、金蝶订单对接等）保持原值即可。

### 步骤 7：启动

```bash
cd E:\AIpaddle
run.bat
```

或手动：

```bash
set PYTHONUTF8=1
set OCR_DEVICE=gpu
.venv\Scripts\python.exe -m uvicorn app.server:app --host 127.0.0.1 --port 8000
```

浏览器打开 http://127.0.0.1:8000 ，用 `.env` 里的 `ADMIN_USER`/`ADMIN_PASSWORD` 登录。

**启动后应该看到两个进程**：

```bash
netstat -ano | findstr LISTENING | findstr "8000 8811"
# :8000  主服务
# :8811  相机子进程（由主服务自动拉起）
```

---

## 四、架构要点（排查问题时需要知道）

### 相机 SDK 隔离在子进程

```
主服务进程 (:8000)  ──localhost HTTP──>  相机子进程 (:8811)
  瘦客户端转发                              独占海康 SDK
  cam_supervisor 监督/秒级重启              持续 grab 最新帧
```

**为什么这么做**：海康 SDK 某些调用在坏会话下会卡死在 native 层、不按 timeout 返回，持锁线程永不释放 → 预览/抓拍全堵死，只能重启整个服务。隔离到子进程后，卡死由看门狗杀掉重启，主服务和网页无感。

**排查相机问题的顺序**：
1. 两个进程都在吗（`netstat` 看 8000 和 8811）
2. 看 `logs/camera.log`（子进程独立日志）
3. **查网卡链路速度** —— 曾经掉到 10Mbps 导致相机取帧超时/掉线。工业相机需要**千兆**，网页顶栏会显示网卡速率并在低于 1Gbps 时标红告警

### 识别模式：模板 vs 规则

- **模板识别**（当前默认）：按型号模板的固定归一化坐标逐框识别。准，且**只有模板模式才拆 4 根槽位**、才会读 PCB 日期框
- **规则识别**：整图自动找日期，不需要模板，但**不拆槽**（整盘当一根），PCB 日期读不到

模板存在 `app/recognition/templates/<id>.json`，就是一堆归一化坐标（0~1 比例），没有图片。当前只有一个模板 `samsung-m321r8ga0pb0-0019`：

```
正面  44 框（全部 dram）
反面  84 框（80 dram + 4 pcb 日期框）
```

> **模板 JSON 丢了要重建，很费时**，务必拷贝 `app/recognition/templates/` 整个目录。

### 性能基准（当前机器实测，可作为新机器的对照）

```
总墙钟 ≈10s / 盘
├─ 占位检测+裁图  0.56s   串行前置（纯 Canny）
└─ 两分支并行     取较慢者
    ├─ OCR 日期识别  ≈5.0s   本地 GPU，128 框全部读出
    └─ 大模型质检    8~13s   随网络波动
```

新机器如果明显慢于这个，按这几点查：GPU 是否真的启用、检测分辨率 `OCR_DET_SIDE_LEN`（当前 1536）、网络到阿里云百炼的延迟。

---

## 五、换机后必做的现场标定

### 静止即拍阈值（**必做，否则可能完全不触发**）

原理：盯相机预览帧算相邻帧的平均绝对差（帧差）。状态机是

```
等放盘 →(帧差>MOTION)→ 等静止 →(连续4帧 帧差<STILL)→ 拍照 → 已拍锁定 →(换盘)→ 等放盘
```

**换机/换光照/换曝光增益后传感器底噪会变**，如果 `AUTO_STILL_THR` **低于底噪**，就永远数不满静止帧，**状态机死卡在"等静止"，一盘都拍不了**（这个坑在当前机器上真实发生过：底噪从 0.74 涨到 1.68，而阈值是 1.5）。

标定方法：

1. 网页点「开始质检」，**画面里什么都不动**
2. 看诊断面板的**底噪**数值（或 `curl http://127.0.0.1:8000/api/hik/auto/status` 看 `noise`）
3. 按底噪设置：
   - `AUTO_STILL_THR` ≈ 底噪 × 1.55
   - `AUTO_MOTION_THR` ≈ 底噪 × 2.4
4. 改 `.env` 后**重启服务**（阈值在模块 import 时读取）

诊断面板会直接给结论，比如底噪压过阈值时会橙底告警：
> 底噪 1.68 ≥ 静止阈值 1.5：永远判不到静止，需把 AUTO_STILL_THR 调到底噪之上(建议 2.6)

### 相机曝光/增益

`.env` 里有持久化的曝光增益值。换机后如果画面过暗/过曝，在网页「🎥 相机预览 / 调试」面板里调，设置服务端全局生效、拍摄自动采用。

> 注意：增益调高会放大传感器噪声，**调完必须重新标定静止即拍阈值**。

### 模板框位

模板是固定坐标，**托盘位置、相机高度/角度变了就会滑框**（框压到旁边元器件上，读到错误数字）。当前已知 slot3 的 PCB 日期框在部分照片上会偏移、读成错误值。

换机重新架设相机后，如果发现日期读错，就需要重建模板：

```bash
set PYTHONPATH=E:\AIpaddle
.venv\Scripts\python.exe tools\build_template.py uploads\<新拍的>\front.jpg --side front --id <新id> --brand Samsung --model M321R8GA0PB0 --sticks 4
.venv\Scripts\python.exe tools\build_template.py uploads\<新拍的>\back.jpg  --side back  --id <新id> --sticks 4 --merge
```

> **注意**：`build_template.py` 靠先跑一遍 OCR 找到日期、再反过来记框位，所以**图暗/对比度差时会找不全框**（实测某张图只找到 11 框，正常应是 80）。而且它**不会自动框出 PCB 板上的日期**（低对比度倒印丝印检不出），需要手动补。目前**没有可视化框选界面**，只能改 JSON 坐标 —— 这是已知的功能缺口。

---

## 六、已知问题与遗留事项

| 问题 | 影响 | 备注 |
|---|---|---|
| **PCB 日期框位不稳** | slot1/slot3 有时读成 2517/2628（应为 2543） | 框压在边界上，压到旁边元器件料号。需人工校准坐标 |
| **没有模板可视化编辑界面** | 调框位只能手改 JSON | 已确认是功能缺口，建议后续补一个框选页面 |
| **`pyproject.toml` 依赖为空** | 不能靠它装依赖 | 已导出 `requirements-full.txt` 替代 |
| **没有 git 远端** | 不能 clone 迁移 | 只能整目录拷贝。建议新机器上建远端 |
| **大模型账号会欠费** | 外观质检全废、PCB 日期一半读错 | 当前机器发生过（400 Access denied）。日期已优化到不依赖大模型，但外观质检必须它 |
| **`models/` 166MB 未加 .gitignore** | 提交会让仓库膨胀 | 待决定：提交 / 忽略 / 用 Git LFS |

---

## 七、快速验收清单

新机器部署完，按顺序验证：

- [ ] `paddle.is_compiled_with_cuda()` 返回 `True`，设备 `gpu:0`
- [ ] `netstat` 能看到 :8000 和 :8811 两个端口
- [ ] 网页能登录，顶栏显示「● 已连 2 台」且网卡 **1000Mbps**（低于千兆会掉线）
- [ ] 「🎥 相机预览 / 调试」能看到上下两路实时画面
- [ ] 放一盘 → 点「开始质检」→ 诊断面板底噪 < 静止阈值 → 能自动触发拍照
- [ ] 识别结果：4 个槽位灯都亮、逐根有判定、PCB 日期读出
- [ ] 总耗时 ≈10s/盘（明显慢很多就查 GPU/网络）
- [ ] 数据能入库（网页「质检管理看板」能看到记录）

---

## 八、参考文件

| 文件 | 内容 |
|---|---|
| [需求文档.md](需求文档.md) | 业务需求 |
| [硬件清单.md](硬件清单.md) | 相机/工控机等硬件 |
| [痛点清单.md](痛点清单.md) | 已知痛点 |
| [README.md](README.md) | 项目说明 |
| `requirements-full.txt` | 完整依赖清单（本次导出） |
