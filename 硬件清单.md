# 云小圈 AI 硬件质检系统 · 硬件清单

> 从「放盘」到「亮灯」的物理 → 软件链路。最后更新：2026-07-15。软件/需求细节见 [需求文档.md](需求文档.md)、[README.md](README.md)。

## 一、硬件清单（BOM）

| # | 硬件 | 型号 / 规格 | 数量 | 作用 | 状态 |
|---|---|---|---|---|---|
| 1 | **计算主机** | GPU RTX3080 / CUDA 12.6 / Win | 1 | 跑 PaddleOCR + 大模型 + 服务端 + MySQL | ✅ 已有 |
| 2 | **上相机** | 海康 **MV-CS050-10GC**（GigE 网口彩色 5MP 2448×2048 全局快门） | 1 | 拍托盘**上面**=front：颗粒 + 主控 + 标签SN | ✅ 已买 |
| 3 | **下相机** | 海康 **MV-CS050-10GC**（同型号） | 1 | 拍托盘**下面**=back：颗粒 + PCB 日期；不翻盘 | ✅ 已买（软件待接第二台/上下绑定） |
| 4 | **镜头 ×2** | 海康 FA **HV1050M-6MP**，10–50mm 手动变焦，600万像素 | 2 | 成像；现场调好视场/对焦后**锁死** | ✅ 已买 |
| 5 | **光源（打光）** | 条形斜射 / 环形（可调亮度） | 1~2 | 让激光丝印立体、压反光——**准确率第一杠杆** | ✅ 已买 |
| 6 | **遮光布** | 遮光 / 挡光布 | 1 | 遮挡环境杂光，稳定光照 | ✅ 已买 |
| 7 | **固定治具 / 机架** | 上下相机对置、托盘居中 | 1 | 固定 相机+镜头+光源+托盘，取景一致 | 🔄 **已固定，未锁死**（碰一下会移位→固定框错位，需防撞/加固） |
| 8 | **托盘** | 4 槽固定卡位（建议不对称卡口防呆） | 若干 | 放 4 根内存条 | ✅ 已做 |
| 9 | **网络交换机** | GigE 千兆交换机 | 1 | 两台相机 → 交换机 → **直连电脑** | ✅ 已买 |
| 10 | **触发方式** | **静止即拍（纯软件，无需硬件）** | — | 放盘后画面静止 → 触发**两台相机同时**拍 | 🔄 定稿，待写 `motion_trigger.py` |
| 11 | **SN 识别方式** | 标签二维码解码（照片直接解 DataMatrix） | — | 读 SN，精确、**无需扫码枪硬件**；解不出用大模型兜底 | ✅ 已实现 |
| 12 | **指示灯** | 可编程 RGB 灯（软件控色），4 个对齐 4 槽 | 4 | 每槽一灯：合格🟢/不合格🔴/识别中🟡/空位⚫ | 🔄 已买待到货 |

**指示灯方案**：判定归软件、灯只听软件——用**可编程 RGB 灯由软件设颜色**（合格绿 / 不合格红 / 识别中黄 / 空位灭），4 个对齐 4 槽，哪根不合格头顶那颗就红。PC 侧统一输出"每槽颜色"、留一个接口给灯；灯板加**看门狗**（超时没收到指令 → 回安全态，不停在旧结果）。灯已采购（在途未到），到货填端口即用。

---

## 二、一盘走一遍（物理 → 软件 → 物理）

1. **放盘**：放一盘 4 根到托盘固定卡位（防呆卡口保证方向）。
2. **触发（静止即拍）**：放盘后画面静止 → 自动触发 上、下两台相机**同时**各拍一张（上=front 面、下=back 面）。
3. **落盘**：两张图按约定命名（区分上/下 + 同盘）写入主机 `WATCH_DIR`。
4. **识别中**：指示灯全**黄**（`BUSY`）。
5. **识别 + 判定**：整图固定模板 → 空位检测（哪些槽有条）→ 每根**逐颗判定不跨根** + 外观 + 读 SN。
6. **拆分入库**：一盘拆 **N 条记录**，各自 SN/日期/合格/归批，写 MySQL + 归档图。
7. **亮灯**：按 `slot_pos` 每根一个灯——合格🟢 / 不合格🔴 / 空位⚫；屏幕工位屏同步逐根显示。
8. **取盘**：取走后灯灭（`CLEAR`），放下一盘循环。

---

## 三、触发方式：静止即拍（软件实现，无需额外硬件）

**纯代码做**，不用按钮/开关/PLC。相机本就在连续预览取流，加一个"运动 → 静止"检测即可：

- **原理**：后台抓预览帧，算相邻帧差（灰度下采样后的平均绝对差）。放盘时画面动（帧差大），放稳后连续 N 帧（≈0.5~1s）帧差低于阈值 = **静止** → 自动触发两台拍照 → 走识别入库。
- **状态机（防连拍 / 防手还在画面里）**：
  ```
  待机 →(检测到明显运动:放盘/手进入)→ 等待静止 →(连续N帧静止)→ 触发拍照+识别
        → 已拍(锁定) →(再次检测到运动:取盘/换盘)→ 回到待机 重新武装
  ```
  关键：拍完进入"已拍锁定"，必须先看到**取盘的运动**才重新武装，否则同一盘会被反复拍。
- **参数**：帧差阈值、静止持续帧数、运动触发阈值、拍后冷却时间——都做成 `.env` 可调，现场标定。
- **代码接入**：新增 `app/pipeline/motion_trigger.py`（后台线程盯预览帧，命中就调现有"拍照 → 识别 → 入库"链）；`.env` 开关 `AUTO_TRIGGER=1` + 阈值参数。不启用就沿用手动。
- **利弊**：省一切触发硬件、体验最顺（放上去就自动拍）；但要防"手还在画面 / 光照抖动"误触发——靠"运动后再静止 + 冷却 + 已拍锁定"三重防抖。可再加**图像质量门禁**（拍完清晰度不够就丢弃重来）。

---

## 四、防重复放盘（方案）

**要解决的问题**：用"静止自动拍"后，同一盘被取下又放上、或第1盘和第3盘其实是同一盘，会被**重复测、重复入库** → 数据库多出重复记录、良率和计数虚高、追溯混乱。

**怎么认出"是同一盘"**：
- **主要靠 SN**：每盘 4 根各有唯一的 SN。系统**记住这一批里已经测过哪些盘**（每盘记下它的 4 个 SN）。新放一盘读出 4 个 SN，如果和本批**已经测过的某一盘 4 个 SN 完全一样** → 就是同一盘重复了。（隔多少盘都能认出，不只是紧挨着的上一盘，所以第1==第3也能抓。）
- **读不到 SN 时的备用**：万一 SN 没读到/读错，就把这盘照片和本批之前测过的照片比一下，**几乎一模一样**也当重复。

**判成重复后怎么办**：默认**跳过、不入库**，屏幕提示「这盘本批已测过，已跳过；确实要复检请点确认」；确认后才存，并标为"复检"。

**不会误伤**：
- 换成别的条 → SN 不一样 → 不算重复。
- 4 根在托盘里换了位置 → 还是那 4 个 SN → 仍算同一盘（对）。
- **只在"当前这一批"里查重**；登记新批次就清零重新开始。所以隔天正常的复检（收货/上架/出货那种）是**新的一批**，不受影响。

**前提**：SN 要读得准——由**标签二维码解码**精确拿到（无需扫码枪），越准查重越靠谱；解不出才退回"比照片"兜底。（列入待办「防重复入库」。）

---

## 五、进度 / 待采购清单

**已买 / 已做**
- [x] 上、下两台相机：海康 **MV-CS050-10GC**（GigE 网口彩色 5MP）
- [x] 镜头 ×2：海康 FA **HV1050M-6MP**（10–50mm 手动变焦，600万像素）
- [x] 光源（打光）
- [x] 遮光布
- [x] 托盘（4 槽固定卡位）；定制托盘已买、在途
- [x] GigE 千兆交换机（两台相机 → 交换机 → 直连电脑）
- [x] 固定治具 / 机架（已固定，**未锁死**——需防撞 / 加固）
- [x] 触发方式定稿：**静止即拍（纯软件，无需硬件）**

**待对接 / 待做**
- [x] SN 识别 = 标签二维码解码（照片直接解，**无需扫码枪**）
- [x] **指示灯**（可编程 RGB，软件控色）已买、在途未到货；到货接上即用
- [ ] 指示灯软件输出（app/indicator.py，待到货联调）
- [ ] 软件：`motion_trigger.py`（静止即拍）、指示灯输出、防重复入库

> 硬件基本齐了（相机 / 镜头 / 光源 / 遮光布 / 托盘 / 交换机 / 治具都到位）；现在第一杠杆是**镜头对焦 + 打光 + 治具锁死**——把 HV1050M 的手动变焦在治具上调到"整盘 4 根全覆盖且颗粒小字清晰"后锁死，配合光源 + 遮光布压反光，识别准确率就能顶上去。
