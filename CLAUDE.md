# CLAUDE.md — 云小圈 AI 硬件质检系统

> 给 AI 助手的项目上下文。**这份文件随项目走**，换机后自动生效。
> 记录的是"看代码看不出来的东西"：业务铁律、踩过的坑、用户的工作偏好。
> 最后更新：2026-07-30

---

## 一、与用户协作的规则（必须遵守）

### 全程用中文
所有面向用户的文字一律中文——回复正文、方案说明、结果总结。代码、字段名、日志保持原样。
> 用户明确要求「全程要用中文和我交流」。

### 不要自己启动或重启服务
不要自己拉起 uvicorn/后端，也不要因为改了代码就自动重启。**需要重启时用户会主动说。**
> 用户明确表示「你不要每次都重启，我需要你重启的时候会和你说」。此前反复自启堆积过多个进程。

改完代码想验证运行态，先 `curl /api/health` 看是否在跑；没跑或需重启就**提示用户**，别自己动手。

### 文档仅按需更新
不要每次改完代码就自动更新 `README.md` / `需求文档.md` / 里程碑。**仅当用户明确要求时才更新。**
> 用户明确表示「你不要每次都自动更新文档，我让你更新你再更新」。

### 管理员账号（调 API 用）
本地服务 `http://127.0.0.1:8000` 管理员：**admin / admin123**

调需要登录的 API 前，先 `POST /api/login`（form 表单，不是 JSON：`-F "username=admin" -F "password=admin123"`）拿 cookie，再带 cookie 调业务接口。

---

## 二、业务铁律（**违反会造成漏判，绝不可妥协**）

总原则：**宁可误报（转人工复查），绝不漏判。**

### 日期判定禁用多数表决
**严禁**用"多数表决 / 剔除离群值"的思路，以后也不要向用户提这个方法。

> **为什么**：质检要防的正是"有人偷换其中单颗存储芯片"（换成不同批次/日期）。多数表决会把这颗被换的芯片当成"误读离群值"吸收掉、放它过关 —— 这是最严重的漏判。

**做法**：必须**逐颗看清每一颗的日期**，所有能读出的日期**逐颗**比较，任何一颗与其余超差 → 判不合格并**定位到具体第几颗**。读不清/遮挡的颗粒是"盲点"（可能藏着被换的芯片）→ 必须标出来转人工确认，不能默默放过。

实现见 `app/services.py::compute_signal`。

### SN 只认二维码精确解码
SN **只能**来自标签二维码（DataMatrix）的 zxing 精确解码（带纠错、结果唯一）。解不出时 **SN 留空 + 标"待人工补录"，绝不采纳大模型读出的 SN。**

> **为什么**：SN 是追溯/去重/防偷换的主键。实测同一根连拍，大模型把 SN 忽读成 `80CE…` 忽读成部件号 `M321…`、型号忽 `EB0` 忽 `EB2` —— 大模型读印刷文字会变、会猜。主键靠猜，一旦错，追溯和防偷换全崩。

**做法**：`app/services.py::_read_label` —— zxing 解出才用（`src='barcode'`）；解不出则大模型只补品牌/型号/频率作参考，`sn=""` + `sn_unread=True`。`build_record` 里 SN 空 → verdict 不能 pass（降级 unknown）+ fail_desc 记"SN二维码未解出待人工"。前端 SN 框：解出=绿(可信)、未解出=红+留空。

**根因常是二维码没拍清** → 上游要靠对焦/打光/清洁把二维码拍清晰。

### 正反槽位对齐 —— 不要动 `HIK_BACK_ORIENT=rot180`
反面的 180° 旋转**不只是让文字转正**，它同时反转左右，使正/反两图槽位都按同一"物理左→右"排序，从而**正面第 i 槽 = 反面第 i 槽**（identity 对应）。

改成 `none`/`flipv` 会导致正反左右镜像错配 —— **第 1 根的正面配上第 4 根的反面**，判定全错。
（L-R 对齐的朝向 = {rot180, fliph} → identity；none/flipv 则正 i 对反 N-1-i。）

---

## 三、环境约束（非显而易见）

- 系统默认 Python 是 3.14，但 **PaddlePaddle 没有 3.14 wheel**。必须用 `uv` 创建的 **.venv (Python 3.12)**，不要用系统 python。
- 环境是 **uv** 管的，**.venv 里没有 pip**。用 `uv pip list` / `uv pip install`。
- 本机网络对 `files.pythonhosted.org` 有 MITM 代理拦截。装依赖须走国内镜像：`-i https://mirrors.aliyun.com/pypi/simple/`；paddle 本体走 `https://www.paddlepaddle.org.cn/packages/stable/cu126/`。
- 已装：paddlepaddle-gpu 3.3.1 + paddleocr 3.7.0（用 PP-OCRv5_server_det/rec）。GPU RTX 3080，CUDA=True。CUDNN 版本不匹配只是警告，不影响。
- **数据库是 MySQL**（不是 SQLite），配置读 `.env` 的 `MYSQL_HOST/PORT/USER/PASSWORD/DB`。
- **OCR 模型已放进项目 `models/`**（166MB，det 85MB + rec 82MB），随项目走、离线可用。`ocr_engine._model_dir()` 检查项目内目录存在就用，否则回退 `~/.paddlex` 缓存（会联网下载）。
- 启动：`run.bat` 或 `.venv\Scripts\python.exe -m uvicorn app.server:app --port 8000`
- 环境变量：`OCR_DEVICE=cpu`（无 N 卡时）/ `OCR_SERVER_MODELS=0`（轻量更快）/ `OCR_DET_SIDE_LEN`（默认 1536）

**显存占用很小**（实测）：净占用 641MB（3.1% of 20GB）、GPU 利用率峰值 56%。所以 6GB 显存的卡就够，瓶颈在逐框调度不在算力。

---

## 四、架构要点

### 相机 SDK 隔离在独立子进程

```
主服务 (:8000)  ──localhost HTTP──>  相机子进程 (:8811)
瘦客户端转发                          独占海康 SDK
cam_supervisor 监督/秒级重启          后台线程持续 grab 写缓冲
```

> **为什么**：海康 SDK 某些调用在坏会话下卡死在 native 层、不按 timeout 返回，持锁线程永不释放 → 预览/抓拍全堵死（表现为 "HTTP 200 但 0 帧"），只能重启整个服务。隔离后卡死由看门狗杀掉重启，主服务/网页无感。

- `app/cameras/hik_sdk.py` — 原 SDK 逻辑，**仅子进程 import**
- `app/cameras/cam_service.py` — 子进程入口，独立 uvicorn 绑 `127.0.0.1:${CAM_PORT:-8811}`。每台相机一个后台 grab 线程持续抓帧写 per-role 缓冲；预览/取帧/双拍全从缓冲读，**请求线程永不碰 SDK**。进程级看门狗连续失败 → `os._exit(3)`。独立日志 `logs/camera.log`
- `app/cameras/hik_camera.py` — **瘦客户端**，公开函数签名全不变，用 urllib 转发。`get_camera(role)` 返回 `_ClientCam`
- `app/cameras/cam_supervisor.py` — 主服务内监督，Popen 拉起 + 每 5s ping /status，连续 3 次无响应或进程退出 → taskkill 重启（退避 + 20s 热身宽限）
- `routers/cameras.py`、`motion_trigger.py` **零改动**（走客户端同名接口）

**排查相机问题的顺序**：① 两个进程都在吗（`netstat` 看 :8000 :8811）② `logs/camera.log` ③ 子进程 `/status` 的 `grab.{role}.fresh/fails/age`。调参释放相机走 `/pause`。

### 识别模式：模板 vs 规则

- **模板识别**（默认）：按型号模板的固定归一化坐标逐框识别。**只有模板模式才拆 4 根槽位、才读 PCB 日期框**
- **规则识别**：整图自动找日期，不需模板，但**不拆槽**（整盘当一根），PCB 日期读不到

模板存在 `app/recognition/templates/<id>.json`，**就是一堆归一化坐标（0~1 比例），没有图片**。`box` 是 4 个角点，`type` 决定解码方式（`dram` 读 3 位 YWW、`pcb`/`controller` 读 4 位 YYWW）。

当前模板 `samsung-m321r8ga0pb0-0019`：正面 44 框（全 dram）、反面 84 框（80 dram + **4 个 pcb 日期框**）。

> **模板 JSON 丢了要重建、很费时**，务必保留 `app/recognition/templates/`。

### 性能基准（0045 实测，可作对照）

```
总墙钟 ≈10s / 盘
├─ 占位检测+裁图  0.56s   串行前置（纯 Canny）
└─ 两分支【并行】  取较慢者
    ├─ OCR 日期识别  ≈5.0s   本地 GPU，128 框全部读出
    └─ 大模型质检    8~13s   随网络波动
```

---

## 五、踩过的坑（**别重复踩**）

### 静止即拍阈值低于底噪 → 永远不触发
状态机：`等放盘 →(帧差>MOTION)→ 等静止 →(连续4帧 帧差<STILL)→ 拍照 → 已拍锁定 →(换盘)→ 等放盘`

**如果 `AUTO_STILL_THR` 低于画面底噪，就永远数不满静止帧，状态机死卡在"等静止"，一盘都拍不了。**

> 真实发生过：增益调到 6.5dB 后传感器底噪从 0.74 涨到 **1.68**，而阈值还是 1.5 → 卡死 `arming`、`count=0`、`frames` 一直涨（说明取帧正常），排查半天。

**标定方法**：画面什么都不动，看 `/api/hik/auto/status` 的 `noise`，然后 `STILL ≈ 底噪×1.55`、`MOTION ≈ 底噪×2.4`。改 `.env` 后**必须重启**（阈值在模块 import 时读取）。

当前值：`AUTO_MOTION_THR=4.0`、`AUTO_STILL_THR=2.6`（底噪 1.68）。**换机/换光照/换曝光增益后必须重标。**

诊断面板会直接给结论并橙底告警，还会算出建议值。

### 网卡链路速度掉到 10Mbps → 相机掉线
出现"取帧超时 `0x80000007` / 枚举不到 / 抓拍 `0x80000214` 锁死 / 预览几帧后卡死"时，**第一步查网卡链路速度**：`Get-NetAdapter`。

> 曾出现重插网线后链路掉到 **10Mbps**（应为 1Gbps）—— 10Mbps 下一台 5MP 相机每帧 400ms+、抓 8 帧就断流。换好网线/换交换机口 → 恢复 1Gbps → 一切正常。**这是硬件/线材问题，不是代码。**

两台相机（上=正面 `DB1224590` / 下=反面 `DB1623157`，**按 SN 绑定不认插口**）经一个交换机接一块网卡（169.254.x.x link-local），双流+连拍时脆弱。网页顶栏会显示网卡速率并在 <1Gbps 时标红告警。

**治本**：两台各走一个网口（各独享千兆），或静态 IP 同网段 + 开巨帧。

软件调优开关（`.env`）：`HIK_FPS`（默认 5，静态托盘足够、省带宽）/ `HIK_PREVIEW_FPS` / `HIK_GEVSCPD`（包间延迟，默认 0；**设 1000 会把吞吐打到 <1fps**）/ `HIK_THROUGHPUT_BPS`。抓拍掉线自愈（close 后等 0.8/1.5/2.5s 再重开）；**预览流不自愈**（每帧 close/open 会 orphan 句柄 + 重开风暴，已回退，掉线交前端重连）。

### 相机断电后曝光回默认 → 画面全黑 → 一切失灵
> 上相机曾断电后曝光回到 5ms → 画面全黑 → 帧差≈0 → 静止即拍/识别全失灵，排查半天才发现是曝光不是相机坏。

已加 `.env` 持久化并在 `open()` 时自动应用：`HIK_FRONT_EXPOSURE_US` / `HIK_FRONT_GAIN` / `HIK_BACK_EXPOSURE_US` / `HIK_BACK_GAIN`。

**相机"看不见/不触发"时，先 capture 一张看亮度均值**（全黑 = 曝光问题）。

### open() 内调用会抢同一把锁的方法 → 自死锁、无日志静默卡死
`HikCamera.open()` 常在调用方**已持有** `self._lock` 时被触发（`grab_array`/`snapshot` 在 `with self._lock:` 内首次调 open）。`self._lock` 是**非重入**的 `threading.Lock`，所以 open() 内**绝不能**调用任何"会自己 `with self._lock:`"的方法（如公共版 `set_exposure`/`set_gain`）。

> 同线程二次获取同一把锁 = 永久自死锁。两路预览线程双双卡在 set_exposure，画面永远不出、**且无任何报错日志**（表现为"预览流开始"后长时间静默）。排查数轮才用 py-spy 实锤。

**做法**：open() 内走**不加锁私有版** `_set_exposure_locked` / `_set_gain_locked`。

**诊断"无日志静默卡死"最快手段**：`uv pip install py-spy` 后 `py-spy dump --pid <PID>` 看每个线程卡在哪一行。

（注：SDK 已隔离到子进程，此坑的排查思路被子进程架构取代，但锁的原则仍适用。）

### 大模型账号欠费 → 静默降级、读数变差还拖慢链路
> 曾遇 Qwen-VL 返回 `400 Access denied, please make sure your account is in good standing`。后果：外观质检全废（四根 verdict 全 unknown），PCB 日期一半读错（原本靠 VL 兜底纠正的 slot 退化），**而且每个待兜底的框还要白等一次网络超时，反而更慢**。

日期识别现已优化到**不依赖大模型**（128 框 100% 由 OCR 读出），但**外观质检必须大模型**。发现质检结果异常时，先确认账号余额。

---

## 六、已被证伪的方案（**别再提议**）

### 整盘一次调用做外观质检 —— 实测更慢
想法：现在逐根调 4 次，改成一次发整盘图判 4 根，省 3 次固定开销。

**实测结果**：
```
逐根并行(裁图 264×2048/168KB)   11.63s   ← 最快
整盘一次(缩到 1280)             15.72s
整盘一次(原图 2448)             20.62s
```

**为什么**：上传数据量 + 大图推理才是主导因素，不是调用次数。逐根裁图每张极窄、像素总量小，并行后墙钟最短。而且整盘输出有**排序串位风险**，会把某根的结论安到别根上（放过坏条），与"逐根独立判定"相悖。

代码保留在 `quality_inspect.inspect_tray`，`TRAY_INSPECT=1` 可开，但**默认关闭**。

### 云端 OCR API —— 不划算
按模板逐框识别，一盘约 150 次调用。百度云高精度含位置版最低 0.018元/次 → **每盘 2.7 元、1000 盘 2700 元**，而本地 5 秒 0 元。且每框一次网络往返会比本地慢、断网即停产、通用 OCR 未必打得过为这个场景调过的本地配置。

### 给颗粒/主控盲试旋转 —— 有害
颗粒和主控是贴片，方向由 SMT 固定，**永远是正的**。盲试旋转白跑一次 OCR，还可能把倒读的错数当成候选（实测 slot1 因此误读 2517）。

**只有 PCB 板丝印是 180° 倒印的**，已在 `_best_read(try_rot180=True)` 里显式处理。

---

## 七、性能优化记录（本次会话，25.20s → 10.06s）

| 步骤 | 耗时 | 说明 |
|---|---|---|
| 起点 | 25.20s | OCR 13.53 + VL 11.11（串行） |
| ① 不试旋转 + 增强优先 | 21.94s | OCR 13.5→9.8，**反面读出率 72/84→84/84** |
| ② 整盘一次调用 | 26~32s | ✗ 证伪，已回退 |
| ③ 正反两面并行 | 15.19s | 原本逐面串行，白等 3.5s |
| ④ 大模型与 OCR 并行 | **10.06s** | 见下 |
| ⑤ 关文本行方向分类器 | OCR 9.0→5.0s | 读出率不变 128/128 |

### ① 增强优先
激光小字/丝印对比度低，原图直读常失败后还要再跑增强，等于每框 3 次 OCR。改成**先 CLAHE 增强读一次**，多数一次命中。实测反面 7.3s→4.7s、读出 72→82。

### ④ 大模型与 OCR 并行（最大收益）
**依赖分析**：外观质检只看元器件/金手指/打磨，**不依赖 OCR 的日期结果**，只依赖"裁好的单根图"；而裁图只需槽位占位框，占位检测是纯 Canny、单独跑仅 **0.10s**。

所以：先花 0.1s 独立算占位框 → 裁图 → 立刻发大模型，**同时**让 OCR 跑日期。两分支并行后取较慢者。

**安全约束**（`services.py` 的 `_pre_crops` / `_vl_branch`）：要求提前占位检测的槽号与最终识别**完全一致**才复用结果，不一致就退回串行重裁。
> 因为裁图/质检结果按槽号存，两次槽号不一致会把**第 2 根的质检结论安到第 3 根**身上 —— 那等于放过坏条。宁可慢，不能错配。

`_pre_crops` 还有多层保险：仅模板模式启用、不足 2 根不启用、任何异常返回 None 退回串行。

### ⑤ 关文本行方向分类器
PaddleOCR 默认 `use_textline_orientation=True`，每识别一个框都跑一个额外的方向分类模型。但颗粒/主控方向固定、PCB 已由 `try_rot180` 显式处理 —— 这个分类器纯属重复劳动。关掉后 OCR 降 45%、读出率不变。

---

## 八、已知问题（待解决）

| 问题 | 影响 | 备注 |
|---|---|---|
| **PCB 日期框位不稳** | slot1/slot3 有时读成 2517/2628（应为 2543） | 框压在边界上、压到旁边元器件料号。模板坐标是基于 0019 那次拍摄标的，托盘位置一变就滑框 |
| **没有模板可视化框选界面** | 调框位只能手改 JSON 坐标 | 用户已提出此需求。`build_template.py` 靠先跑 OCR 再反记框位，图暗时找不全框（实测某图只找到 11 框，应是 80），且**不会自动框出 PCB 日期**（低对比度倒印丝印检不出） |
| `pyproject.toml` 依赖为空 | 装不出依赖 | 已导出 `requirements-full.txt` 替代 |
| **没有 git 远端**、50 文件未提交 | 不能 clone 迁移 | 只能整目录拷贝。见 `交接文档.md` |
| `models/` 166MB 未加 .gitignore | 提交会让仓库膨胀 | **待用户决定**：提交 / 忽略 / Git LFS |

---

## 九、相关文档

| 文件 | 内容 |
|---|---|
| `交接文档.md` | **换机部署完整步骤**（本次编写） |
| `需求文档.md` | 业务需求 |
| `硬件清单.md` | 相机/工控机等硬件 |
| `痛点清单.md` | 已知痛点 |
| `requirements-full.txt` | 完整依赖清单（98 包精确版本） |
