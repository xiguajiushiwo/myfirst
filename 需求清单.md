# 云小圈 AI 硬件质检系统 · 需求清单（颗粒化 · 全量）

> 把整个项目的需求拆到**最小可执行点**，每条一个原子需求 + 状态，兼作覆盖检查表。
> 状态：✅ 已实现 / 🔄 部分或进行中 / ⛔ 待做 / 🚫 已弃用。
> 配套：叙述版 [需求文档.md](需求文档.md)、硬件 [硬件清单.md](硬件清单.md)、技术 [README.md](README.md)。最后更新 2026-07-15。

---

## 0. 总目标
- 0.1 对内存条做自动化质检：拍正反面 → 识别日期码 + 外观 → 判合格/不合格 → 留档追溯。✅
- 0.2 系统名：云小圈AI硬件质检系统。✅
- 0.3 面向：工程/商业/内部自用产线。✅
- 0.4 一盘 4 根一次拍摄识别，每根独立出结果、独立入库。✅

## 1. 硬件（详见硬件清单.md）
- 1.1 计算主机 GPU（RTX3080/CUDA12.6）跑 OCR+大模型+服务端+MySQL。✅
- 1.2 上相机 海康 MV-CS050-10GC（GigE 彩色 5MP 全局快门）拍上面=front。✅ 已买
- 1.3 下相机 同型号 拍下面=back，不翻盘。✅ 已买+已接（SN DB1623157）
- 1.4 镜头 ×2 海康FA HV1050M-6MP（10–50mm 手动变焦），现场调好锁死。✅ 已买
- 1.5 光源（斜射/环形，可调亮度）压反光、让丝印立体。🔄只买了两个条形光（亮度可调）+ 一个环形光（亮度不可调） 
- 1.6 遮光布：遮环境杂光、稳定光照。✅ 已买
- 1.7 固定治具/机架：上下相机对置、托盘居中、取景一致。🔄 已固定未锁死
- 1.8 托盘：4 槽固定卡位。✅ 已做
- 1.9 托盘防呆：不对称卡口或基准标记，防放反/转180°。⛔
- 1.10 GigE 千兆交换机：两相机→交换机→直连电脑。✅ 已买
- 1.11 SN 由**标签二维码照片解码**（无需扫码枪）。✅
- 1.12 指示灯：4 个可编程 RGB，软件控色。🔄 已买待到货
- 1.13 治具锁死/防撞，避免碰移导致固定框错位。⛔

## 2. 上料与拍摄流程
- 2.1 上料方式=托盘，操作员放一盘(4根)→拍→取下→放下一盘。✅
- 2.2 弃用电机传送、弹仓、到位信号双向握手。🚫
- 2.3 正反面=双相机各拍一面（上/下），不翻盘。✅（上DB1224590正/下DB1623157反，按SN绑定）
- 2.4 一次触发两台相机**同时**拍（防错位）。✅（capture_both 两线程并行，实测0.83s）
- 2.5 触发方式=静止即拍（放盘后画面静止自动拍），纯软件。🔄 待写 motion_trigger.py
- 2.6 静止判定：相邻预览帧差低于阈值、连续 N 帧。⛔
- 2.7 状态机：待机→运动→静止→拍→已拍锁定→(取盘运动)→重新武装。⛔
- 2.8 已拍锁定：必须先检测到取盘运动才重新武装，防同盘连拍。⛔
- 2.9 防误触发：运动后再静止 + 拍后冷却 + 已拍锁定 三重防抖。⛔
- 2.10 触发相关阈值/帧数/冷却做成 .env 可调、现场标定。⛔
- 2.11 取盘检测→灭灯（画面变空确认）→放下一盘循环。⛔
- 2.12 图像质量门禁：拍糊/过曝当场判"重拍"，不硬识别出错值。⛔
- 2.13 相机独占：海康一台只能被一个程序打开，两台各自管句柄、异常释放。✅（每台独立句柄+独立锁）
- 2.14 双相机按相机序列号绑定"上/下"，不靠插口顺序。✅（HIK_FRONT_SN/HIK_BACK_SN）
- 2.15 正反两张按"同一盘 + 上/下"约定命名以正确配对。⛔
- 2.16 双相机正反镜像对齐：反面左右镜像，须映射，防正反配错根。⛔

## 3. 图像来料 / 监听
- 3.1 相机把一盘的图写入 WATCH_DIR 下一个新子文件夹。✅
- 3.2 FolderWatchFeeder 监听目录，新子文件夹+图写完(大小稳定+可解码)→自动识别。✅
- 3.3 落 `.done` 标记，重启不重复处理。✅
- 3.4 WATCH_DIR 由 .env 配置。✅
- 3.5 保留手动上传 / 读文件夹 / 本机相机 三种测试输入源。✅

## 4. 识别 · 日期码（三类）
- 4.1 三类：存储颗粒 YWW(3位)、PCB YYWW(4位)、主控/RCD YYWW(4位序列号前缀)。✅
- 4.2 PaddleOCR 3.x（PP-OCRv5 server det/rec），GPU。✅
- 4.3 颗粒 3 位 YWW 解码（首位年个位）。✅
- 4.4 PCB/主控 4 位 YYWW 解码（世纪规则 00-69→20xx / 70-99→19xx）。✅
- 4.5 4 位滑窗防误取（如 K72536 取 2536 而非 7253→1972），优先近年。✅
- 4.6 归一化年周 YYYYWW 入库。✅
- 4.7 原生输出：能解码给日期（误读也照实给），不能解码显示 OCR 原文，完全没读到显"未识别"。✅
- 4.8 绝不显示"遮挡"字样。✅

## 5. 识别 · 模板与规则
- 5.1 整图固定模板：一张图里 4 根的所有颗粒 + 每根 PCB 全框，一次识别。✅
- 5.2 单根模板（按型号固定框）弃用（只放4根一盘）。🚫（旧JSON留档）
- 5.3 规则识别（整图自动找，无模板）作临时兜底，会漏检。✅
- 5.4 模板格式：每面 boxes[type,box(4归一化点),id]，type∈dram/pcb/controller。✅
- 5.5 模板可选 `slots`（每槽归一化矩形，左→右）。✅
- 5.6 模板缺 slots 时按框 x 跨度自动聚类兜底。✅
- 5.7 真机 4 槽整图模板（正/反两套）按真实取景建。⏳ 待真机照片
- 5.8 模板批量自动生成工具。🔄
- 5.9 模板库可管理（增/删/选默认）。✅

## 6. 识别 · 提准（多方案）
- 6.1 自动判方向：原样 vs 翻转180°，取文字置信更高朝向（治倒装芯片）。✅
- 6.2 对比增强 CLAHE 救低对比/发灰小字。✅
- 6.3 字符混淆挽救：SEC/SAMSUNG 锚点后 3 位把形近字母映射回数字（S→5,O→0,I→1,B→8,Z→2,G→6,T→7）。✅
- 6.4 选择优先级：能解码 > 置信高。✅
- 6.5 标注框收紧到只圈数字（去 SEC/前缀字母）。✅
- 6.6 颗粒按空间顺序编号 idx=1..N（上→下、左→右），标注+定位用。✅
- 6.7 逐颗给真实置信度（取该读数所在检测 score）。✅

## 7. 识别 · PCB / 主控
- 7.1 PCB 日期集成到反面识别（不再单独检测）。✅
- 7.2 PCB 4 位框按颗粒同套多方案（翻正+增强）识别。✅
- 7.3 PCB 低置信/未解码转大模型兜底。✅
- 7.4 单独 PCB 上传槽保留（无模板/特写时用）。✅

## 8. 识别 · 大模型（VL）
- 8.1 逐颗低置信/未解码 → 逐颗单图单调用 VL 兜底（线程池并发）。✅
- 8.2 发 VL 前统一预处理：翻正 + 增强（_prep_for_vl）。✅
- 8.3 透明：不覆盖 OCR 原始读数/置信，note 摊开「OCR原文(置信)→大模型读为X」。✅
- 8.4 整图 VL 核对漏检（开关）：VL 数颗粒总数与 OCR 检出比，多≥2 告警疑似漏检。✅
- 8.5 SN/品牌/型号/频率由 VL 读正面标签。✅
- 8.6 外观质检三项由 VL：①元器件(损坏/发黑)②金手指③芯片二维码标记(有线条正常/散点异常)。✅
- 8.7 二维码标记只报有无异常，不报数量/不定位。✅
- 8.8 外观三态 bool + 不合格逐条说明。✅
- 8.9 提示词集中在 app/prompts.py，调词不碰识别逻辑。✅
- 8.10 多 provider 配置（OpenAI 兼容），可接多个模型。✅
- 8.11 一键切换启用 provider。✅
- 8.12 自动降级：启用排序逐个尝试，失败切下一个，全失败才报错、每次降级写日志。✅
- 8.13 用量统计（调用次数/token）+ 单价则估费。✅
- 8.14 默认模型 qwen3-vl-plus（更快）。✅
- 8.15 本地多模态模型（离线/省费用/断网兜底）。⛔
- 8.16 读标签结果缓存、成本统计（断网/欠费不瘫）。⛔

## 9. 综合判定（唯一信号灯）
- 9.1 单一合格/不合格灯：日期与三项外观全合格才绿。✅
- 9.2 日期判据：所有有效日期最大周差 ≤ 阈值。✅
- 9.3 阈值前端可调，默认 10 周。✅
- 9.4 **逐颗比较，严禁多数表决**：任一颗超差即不合格。✅
- 9.5 不合格定位到具体第几颗（防偷换单颗芯片）。✅
- 9.6 盲点（OCR+VL 都读不出）单列提示人工确认，不默默放过。✅
- 9.7 盲点不用"遮挡"字样、不预标位置。✅
- 9.8 有效日期<2 → unknown（无法比周差）。✅
- 9.9 不合格才给 fail_desc，逐条"位置：问题"，定位到 正面第N颗/PCB/主控/元器件/金手指/二维码标记。✅
- 9.10 宁可误报(转人工复查)、绝不漏判。✅

## 10. 一盘拆 N 条记录（逐根）
- 10.1 检测到 ≥2 根 → 按槽位拆成 N 条独立记录。✅
- 10.2 逐根判定只在本根内逐颗比较，**不跨根**（防不同批次两根互相超差误判）。✅
- 10.3 每根各自读 SN（该槽裁图**解二维码**优先、VL 兜底）。✅
- 10.4 每根各自外观质检（该槽裁图单独送 VL）。✅
- 10.5 每根各自 PCB/主控日期、合格判定、不合格定位。✅
- 10.6 每根归入当前批次并继承登记信息。✅
- 10.7 记录 slot_pos（托盘第几槽 1..N，左→右）。✅
- 10.8 空位不产生记录、不算盲点。✅
- 10.9 单根/规则模式仍整图一条记录（旧行为）。✅
- 10.10 每根 2 次 VL(外观+SN)，N 根 2N 次，线程池并发。✅
- 10.11 每根 DateCode 带 slot 字段。✅
- 10.12 管理看板每条记录显示"托盘第N槽"。✅

## 11. 托盘空位检测（先数数量再识别）
- 11.1 识别前先判每个槽有没有条。✅
- 11.2 判据=边缘/纹理密度（空槽光滑→少，有条→多）。✅
- 11.3 阈值 SLOT_PRESENCE_MIN，.env 可调。✅
- 11.4 检测异常时保守判"有条"（宁多识别不漏真条）。✅
- 11.5 空槽整槽跳过（不识别不判定）。✅
- 11.6 空槽标注图画灰框「空位」（不静默跳过）。✅
- 11.7 输出每面 slots(占用/分数) + 本盘 stick_count。✅
- 11.8 阈值标定工具 tools/calibrate_slots.py（满盘/空盘各拍一张）。✅（脚本就绪，待真机标定）
- 11.9 多放/放歪跨槽检测。⛔

## 12. 防重复放盘
- 12.1 认盘主键=一盘 4 根 SN 排序组合（无序）。⛔
- 12.2 维护"当前批次已见集合"，与集合内所有盘比（不止上一盘，第1==第3也抓）。⛔
- 12.3 SN 读不到时用图像相似度兜底。⛔
- 12.4 命中重复→默认跳过不入库，提示"本批已测过，确需复检请确认"。⛔
- 12.5 确认后才存并标"复检"。⛔
- 12.6 作用域=当前批次，登记新批次清零（隔天合法复检不受影响）。⛔
- 12.7 不误伤：换别的条 SN 不同→不重复；换槽顺序→仍同盘。⛔

## 13. 批次登记与继承
- 13.1 每批测试前登记：客户/品牌/容量/频率/品相(拆机·拆新·全新)/备注(来源如香港货·马来货)/批次号(单号)。✅
- 13.2 登记后每根自动归入并带上这些信息。✅
- 13.3 登记为准：品牌/容量/频率/客户/单号取批次，每根只读 SN。✅
- 13.4 可选择已登记批次继续。✅
- 13.5 batches 表（批次号/客户/品牌/容量/频率/时间/在检）。✅
- 13.6 关闭批次。✅

## 14. 数据库
- 14.1 MySQL 库 yunxiaoquan_qc，程序自动建库建表。✅
- 14.2 inspection_records 字段：时间/操作人/SN(不唯一,每次新增留历史)/品牌/型号/频率/主控日期/PCB日期/存储芯片(JSON含序号)/颗粒数/元器件·金手指·二维码 三态bool/日期合格/综合判定/不合格说明/复查状态。✅
- 14.3 追溯图片字段 front_img/back_img/annotated_front/annotated_back。✅
- 14.4 批次字段 customer/capacity/batch_no/cond/remark。✅
- 14.5 slot_pos 字段（托盘槽位）。✅
- 14.6 operators 表（操作人增删选）。✅
- 14.7 品牌/型号/频率/SN 由 VL 读标签自动填、可人工改。✅
- 14.8 增量迁移：缺列 ALTER 补，不 DROP、改表不丢历史数据。✅

## 15. 追溯 / 归档 / 审计 / 备份
- 15.1 原图+标注图归档到永久区 archive/<日期>/<uid>/，记录字段改写为 /archive URL。✅
- 15.2 归档幂等（已是 /archive 跳过、缺文件置空）。✅
- 15.3 uploads 被清后仍可追溯。✅
- 15.4 审计表 audit_log，改复查/删模板/删操作人写审计。✅
- 15.5 审计可查询。✅
- 15.6 数据库定时备份脚本（mysqldump→backups/，保留N天）。✅

## 16. 良品率看板 / 统计
- 16.1 今日/累计良率 KPI（verdict=pass 为良品）。✅
- 16.2 按客户良率。✅
- 16.3 按批次（已检/良品/良率）。✅
- 16.4 记录明细多条件筛选（客户/批次/判定/日期/关键词）。✅
- 16.5 独立页 /manage。✅
- 16.6 良率按天/周趋势 + 骤降告警。⛔
- 16.7 不良类型分析（日期超差/外观/盲点占比 Top）。⛔

## 17. 记录 · 复查 · 处置
- 17.1 人工复查：可改"复查合格/复查不合格"。✅
- 17.2 复查动线顺手（误报靠人工兜底）。🔄
- 17.3 不合格处置闭环（返工/隔离/报废 状态流转）。⛔
- 17.4 记录可回看归档图。✅

## 18. 数据价值挖掘
- 18.1 按 SN 追溯举证：输/扫 SN → 调出该根历次记录 + 当时原图/标注图/判定/时间。⛔
- 18.2 同 SN 历史比对：多次质检颗粒日期变了→中途被换芯片告警（防偷换第二道）。⛔
- 18.3 来源/品相 质量画像：按渠道/品相统计不良率。⛔
- 18.4 报表导出 Excel/PDF（按批次/客户/日期，对账/质检报告）。⛔
- 18.5 与交易系统(云小圈)打通，质检结果回流。⛔

## 19. 前端页面
- 19.1 工作台：顶栏+我的菜单+卡片风格；批次登记卡；实时画面卡(预览+拍正反+识别+曝光)；高级参数(模式/vl_check)。✅
- 19.2 工位屏 /station：全屏零点击，喂料自动出结果。✅
- 19.3 工位屏一盘逐根显示 合格/不合格 + SN + 定位 + 正反标注图 + 本批已检/良品/良率。✅
- 19.4 管理看板 /manage。✅
- 19.5 系统设置 /settings：多 provider 配置 + 用量。✅
- 19.6 统一设计系统（app.css）。✅
- 19.7 前端风格贴近云小圈交易同步系统。✅
- 19.8 手动识别页对多根托盘逐根显示（跟工位屏对齐）。⛔
- 19.9 coverage_warn 漏检告警条。✅
- 19.10 一图多根独立页已删（并入整图模板）。🚫

## 20. 指示灯
- 20.1 判定归软件、灯只听软件。✅（原则）
- 20.2 4 个灯对齐 4 槽。⛔
- 20.3 颜色：合格绿/不合格红/识别中黄/空位灭。⛔
- 20.4 用可编程 RGB 灯（软件设色）。⛔
- 20.5 PC 侧统一 app/indicator.py 输出每槽颜色，后端可插拔(null/serial/…)。⛔
- 20.6 协议 BUSY / `1G 2R 3G 4-` / CLEAR。⛔
- 20.7 灯板看门狗：超时回安全态，不停在旧结果。⛔
- 20.8 无灯/断开时空转，不影响识别入库。⛔
- 20.9 接入点：analyze_and_save 进 busy()、出 show()。⛔

## 21. SN 识别（标签二维码解码，无需扫码枪）
- 21.1 SN 是追溯与拆记录的主键，须可信。✅（原则）
- 21.2 SN 由**标签二维码解码**（zxing-cpp，照片直接解），大模型兜底。✅
- 21.3 无需扫码枪硬件；4 根按槽裁图**逐根解码、各归各根**。✅

## 22. 运维 / 工程化
- 22.1 分级滚动日志（logs/app.log，留N天）。✅
- 22.2 请求日志中间件（方法/路径/状态/耗时，高频降噪）。✅
- 22.3 健康检查 /api/health（db_ok/hik相机/大模型key/版本）。✅
- 22.4 运行指标 /api/metrics（处理数/pass/fail/错误/uptime/良率）。✅
- 22.5 进程守护（崩溃自重启 + 开机自启脚本）。✅
- 22.6 监控告警（相机掉线/大模型超时欠费/DB挂/良率骤降）。⛔
- 22.7 断电/断网恢复。⛔
- 22.8 部署 runbook / Docker 一键部署。⛔

## 23. 安全 / 权限
- 23.1 登录 + 角色鉴权（质检员/主管/管理员）。⛔ 暂缓
- 23.2 写/管理类接口守护。⛔
- 23.3 内网 HTTP 可接受，正规交付收口。⛔

## 24. 测试 / 评测
- 24.1 单元测试 pytest（27 例，0.3s）。✅
- 24.2 date_parser 解码测试。✅
- 24.3 compute_signal 判定测试（同批pass/阈值边界/单颗被换必fail并定位/盲点/有效<2）。✅
- 24.4 空位检测测试（边缘密度/显式slots/自动聚类/框归槽）。✅
- 24.5 逐根拆分测试（跨根不比较/根内被换只该根fail/盲点按根隔离）。✅
- 24.6 准确率评测脚本（样本目录+truth.json→每颗读对率/错哪颗/判定是否符预期）。✅
- 24.7 空位/拆分/防重复 之后的回归测试持续补充。🔄

## 25. 性能
- 25.1 大模型三路并行（识别/外观/读标签），墙钟≈最慢一路。✅
- 25.2 一整轮 ~7s（并行前 ~15-20s）。✅
- 25.3 产线可用模板模式(OCR 亚秒)、**二维码解码**替代读标签提速。🔄
- 25.4 逐颗 VL 用线程池并发不变慢。✅

## 26. 配置 / 环境
- 26.1 .venv Python3.12（uv 管理，无 pip），依赖走阿里云镜像。✅
- 26.2 GPU 版 paddle（CUDA12.6）。✅
- 26.3 .env：DASHSCOPE_API_KEY(gitignore)/WATCH_DIR/OCR_*/HIK_*/阈值等。✅
- 26.4 .env 向上查找加载。✅
- 26.5 关键阈值全部 .env 可调（OCR_CONF_MIN/SLOT_PRESENCE_MIN/触发参数/INDICATOR_*）。🔄

## 27. 代码结构（约束）
- 27.1 分层：server(入口)/core(路径常量)/services(服务层)/routers(接口分组)/recognition·inspection·cameras·pipeline·storage(领域子包)。✅
- 27.2 接口按职责拆 router（recognition/cameras/records/pipeline/batches/settings）。✅
- 27.3 每次代码改动同步更新 README.md。✅（约定）
- 27.4 每次交流的需求/决策同步写进 需求文档.md。✅（约定）

## 28. 衔接坑 / 边缘情况（必须处理）
- 28.1 双相机正反镜像对齐。⛔
- 28.2 放盘防呆（防放反/转180°）。⛔
- 28.3 图像质量门禁（拍糊/过曝重拍）。⛔
- 28.4 两台同步 + 相机独占。⛔
- 28.5 治具漂移自检（基准点比对，偏移告警重标定）。⛔
- 28.6 SN 可信（**标签二维码解码**，无需扫码枪）。✅
- 28.7 防重复入库（幂等）。⛔
- 28.8 缺盘/少放（空位已处理）/多放。🔄

## 29. 已弃用（留档）
- 29.1 电机传送 / 弹仓连续上料。🚫
- 29.2 到位信号双向握手 / SimulatedFeeder / SerialFeeder。🚫
- 29.3 单根按型号模板（产线路径）。🚫
- 29.4 一图多根独立页 /multi。🚫
- 29.5 "遮挡"字样。🚫
- 29.6 判定多数表决/剔除离群。🚫（铁律禁止）
