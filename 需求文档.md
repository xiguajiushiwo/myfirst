# 云小圈 AI 硬件质检系统 · 需求文档

> 本文档随沟通持续更新，记录需求与已确定的方案决策。最后更新：2026-07-06（海康相机接入、代码分层整理、工程化缺口评估 §八）。
> （技术实现细节见 [README.md](README.md)；本文件只记"要什么、怎么定的"。）

## 一、项目目标

对内存条做自动化质检：拍正反面照片 → ①识别三类日期码 ②外观质检 → 给出**合格/不合格**判定并**留档追溯**。系统名：**云小圈AI硬件质检系统**。

## 二、识别（日期码）

> 本轮更新：**PCB 日期不再单独拍/单独检测，集成到反面**——在反面模板里给 PCB 那 4 位丝印日期(YYWW，如 2536=2025年第36周)也画一个 `type=pcb` 框，和存储颗粒一样：框内 OCR 读 4 位，低置信/读不出就转大模型兜底，标注框收紧到只圈数字。`recognize_side` 现支持 `dram / pcb / controller` 三类框；单独的 `pcb` 上传槽仍保留（无模板/特写时用）。实测 demo2 反面：20 颗 + PCB 框 2536（丝印倒装 OCR 读空→大模型读出）。


- 三类日期码：**存储颗粒** YWW(3位) · **PCB** YYWW(4位) · **主控/RCD** YYWW(4位序列号前缀)。
- 两种识别方式（前端可切换）：
  - **规则识别（默认）**：整图 OCR + 规则在全图自动找日期，不依赖固定框、不需模板。
  - **模板识别**：按品牌/型号模板的固定框套坐标，只识别颗粒。模板库可管理。
- **PCB 走双路综合**：PaddleOCR + Qwen-VL 大模型同时识别再综合（一致采用/只有一方采用那个/不一致取大模型标冲突）——因 PCB 丝印对比度低。
- **原生结果，不预测/不校正**：能解码给日期（误读也照实给），不能解码显示 OCR 原文；完全没读到显示"未识别"。
- **存储颗粒编序号**（每面从上到下、左到右 1..N），标注图上显示，便于定位。
- 计时分项：**日期识别用时(ocr_sec)** 与 **大模型用时(model_sec)** 分开展示。

## 三、外观质检（Qwen-VL 多模态大模型）

- 检查三项，各出一个是否正常的判断：**①元器件**(损坏/发黑) **②金手指** **③存储芯片二维码状标记**(有线条=正常，只有散点=异常)。
- 二维码标记只报**有没有出现异常**，不报具体数量、不定位到第几颗。

## 四、综合判定（唯一信号灯）

- 页面只有**一个**合格/不合格灯：**日期 与 三项外观 全部合格才亮绿灯**，否则红灯。
- 日期合格判据**只有一个指标**：所有有效日期的**最大周差 ≤ 阈值**（阈值前端可调，默认 10 周），> 阈值即不合格。
- **逐颗比较，严禁多数表决**：必须**看清每一颗的日期**，把所有能读出的颗粒日期**逐颗**两两比较；**任何一颗**与其余不一致（超差）→ 判不合格并**定位到具体第几颗**。
  - **为什么不能用多数表决**：质检要防的正是「有人偷换其中单颗存储芯片」（换成不同批次/日期）。若用多数表决/剔除离群，这颗被换的芯片会被当成「误读」吸收放过——这是最严重的**漏判**。质检铁律：**宁可误报（转人工复查），绝不漏判**。
  - **盲点必须显性提示**：OCR 与大模型都读不出日期的颗粒是可能藏着被换芯片的盲点，判定里**单列出来**（`另有 …第N颗 看不清日期，请人工确认`），不默默放过（不用"遮挡"字样、不预标位置）。
  - 代价与对策：逐颗判定下，个别 OCR/大模型误读会造成**误报**（好条被判不合格）——这是可接受的（人工复查兜底），换来的是绝不漏判。真正的解法是**提高拍摄质量**（打光/分辨率/对焦/朝向一致，见 §7.1）把每颗都拍到能读清，以及低置信/盲点项**自动转人工复查**。
- **不合格才给说明(fail_desc)**，逐条"位置：问题"，定位到：正面第N颗颗粒 / PCB / 主控 / 元器件 / 金手指 / 芯片二维码标记；合格不写说明。

## 五、数据库（MySQL 留档）

- 库 `yunxiaoquan_qc`（本地 3306 / root / 123456，程序自动建库建表）。
- 表 `inspection_records`（18 字段）：时间、操作人、SN(唯一Id,**不唯一**,每次质检新增一行留历史)、品牌、型号、频率、主控日期(YYYYWW)、PCB日期(YYYYWW)、存储芯片(JSON含序号)、颗粒数、元器件/金手指/芯片标记 三态bool、日期是否合格、综合判定、不合格说明、**人工复查状态**。
- 表 `operators`（操作人，前端可增删选）。
- **品牌/型号/频率/SN 由大模型读正面标签自动填**，页面可人工改。
- 质检员盯屏复核：AI 出判定，人工可在记录里改「复查合格/复查不合格」。

## 六、采集与部署（职责划分）

**相机端（用户负责）**：改用**带主控芯片、可编程的智能相机/边缘设备**（如 树莓派/Jetson + 相机模组、或工业智能相机）。用户把脚本写进相机，由**相机自己**完成：
- 自动检测（静止 / 红外·光电到位）→ **自动触发拍照**（正反两面）；
- 把拍好的两张照片**送到服务器**（交接方式见七）。

**服务器端（本系统负责）**：接收照片 → 模型分析（日期识别 + 外观质检）→ 综合判定 → 入库 → **看板任意电脑访问**（`http://服务器IP:端口`）。GPU、PaddleOCR、Qwen-VL 都在服务器跑。

**测试期保留三种输入源**（开发验证用，不依赖相机）：手动上传 / 读取文件夹(test_photos/) / 本机 OpenCV 相机。

## 七、【最新定稿 2026-07-14】托盘上料（弃用弹仓/电机/到位握手）

- **上料方式改定为托盘**：一个托盘固定放 **4 根**内存条（固定卡位）；操作员**每次放上新的一排(一盘4根)→ 拍照 → 取下 → 放下一盘**。**不用电机传送、不用弹仓、不用到位信号双向握手**（§7.1 那套弹仓方案作废，留档）。
- 托盘=**天然固定治具** → 4 根位置固定、取景一致 → **只用整图固定模板**（一张图里 4 根 + 各自 PCB 全部框死），一次识别 4 根、各自归批入库。（已用 demo4 三根验证：整图模板 60颗+3PCB 全中；4 根同理，按托盘取景建一次模板即可。）
- **正反面 = 双相机（2026-07-14 定稿）**：一台相机拍正面、一台相机拍反面，**不翻盘**；两台同时/依次出图，正背两张按托盘位配对，一次识别 4 根的正反。
- **单根模板弃用（2026-07-14 定稿）**：因为**只会放 4 根一盘**，不再需要"按型号的单根模板"；识别只走**整图固定模板**（+ 规则识别临时兜底）。原单根模板 JSON 仅留档，不在产线路径上。
- **触发方式定稿 = 静止即拍（纯软件，2026-07-15）**：放盘后画面静止即自动触发上/下两台相机同时拍 → 识别入库。不用按钮/到位开关/定时，无需额外硬件（详见 [硬件清单.md](硬件清单.md) 触发小节；待写 `app/pipeline/motion_trigger.py`）。

## 硬件总装（全链路串联 · 2026-07-15）

> 从「放盘」到「亮灯」的整条物理→软件链路。所有硬件都是**单向配合**（相机出图、灯板收指令），**没有电机/弹仓/双向握手**（已弃用，见 §7.1）。

### H0. 硬件清单（各件的角色）
| # | 硬件 | 作用 | 状态 |
|---|---|---|---|
| 1 | **计算主机**（GPU，RTX3080 / CUDA12.6） | 跑 PaddleOCR + 调大模型 + 服务端 + MySQL；所有软件在这 | ✅ 已有 |
| 2 | **上相机**（海康 **MV-CS050-10GC**，GigE 网口彩色 5MP） | 拍托盘上面=front（颗粒 + 主控 + 标签SN） | ✅ 已定 |
| 3 | **下相机**（同型号 MV-CS050-10GC，第二台） | 拍托盘下面=back（颗粒 + PCB 日期）；**不翻盘** | ✅ 已定（软件待接第2台） |
| 4 | **镜头 ×2** | 海康 FA **HV1050M-6MP**（10–50mm 手动变焦，600万像素）；现场调好后锁死 | ✅ 已定 |
| 5 | **光源（打光）** | 斜射/环形，让激光丝印立体、压反光——**准确率第一杠杆** | ⛔ 待做 |
| 6 | **固定治具/机架（模具）** | 上下相机对置、托盘居中，把 相机+镜头+光源+托盘**锁死**（固定模板前提） | ⛔ 待做 |
| 7 | **托盘** | 4 槽固定卡位放 4 根；建议不对称卡口防呆 | ✅ 已做 |
| 8 | **触发方式** | **静止即拍（纯软件）**：放盘后画面静止 → 触发两台同时拍（无需硬件） | 🔄 定稿，待写 motion_trigger.py |
| 9 | **SN 识别（无需扫码枪）** | 由**正面标签二维码**照片解码拿 SN（精确）；大模型仅兜底 | ✅ 已实现 |
| 10 | **指示灯板**（ESP32/Arduino + 4 颗 WS2812） | 每槽一灯：合格绿/不合格红/空位灭/识别中黄；USB 串口收指令 | ⛔ 待做 |

### H1. 连接拓扑（信号怎么走）
```
                        [固定治具/机架 6]  ← 把 2/3/4/5/7 全部固定
   正面相机2 ─镜头4─┐        │
                   ├─(GigE 网线)→ [计算主机 1] ─(USB串口)→ [指示灯板 10] → 4 颗灯(绿/红)
   反面相机3 ─镜头4─┘        │              │
        光源5(常亮/触发亮)    │              （SN 由正面标签二维码从图里解出，无需扫码枪）
   托盘7(4根) ── 静止即拍8 ───┘(放盘→画面静止→触发两台同时拍)
```
- **相机→主机**：GigE 网线，海康 MVS SDK 抓帧（正面机/反面机按**相机序列号**绑定，别靠插口顺序）。
- **主机→灯板**：USB 串口，PC 每盘发 `1G 2R 3G 4-`（槽1绿 槽2红 槽3绿 槽4空）。
- **SN 来源**：正面标签**二维码解码**（从拍好的正面图直接解，无需扫码枪）；解不出用大模型兜底。
- **触发→相机**：一次触发**两台同时拍**（否则一动就正反错位）。

### H2. 全流程（一盘走一遍：物理 → 软件 → 物理）
1. **放盘**：操作员放一盘 4 根到托盘固定卡位（防呆卡口保证方向）。
2. **触发（静止即拍）**：放盘后画面静止 → 自动触发 上、下两台相机**同时**各拍一张。
3. **落盘**：两张图按约定命名（区分正/反 + 同一盘）写入主机的 `WATCH_DIR`。
4. **识别中**：指示灯板全**黄**闪（`BUSY`）。
5. **识别+判定**（软件，见 §二~§四、§7.5、§7.6）：整图固定模板 → 空位检测（哪些槽有条）→ 每根逐颗判定（不跨根）+ 外观 + 读 SN。
6. **拆分入库**：一盘拆成 **N 条记录**，各自 SN/日期/合格判定/归批，写 MySQL + 归档图。
7. **亮灯**：按 `slot_pos` 组好 4 槽状态发灯板 → **每根一个灯**：合格🟢 / 不合格🔴 / 空位⚫。屏幕工位屏同步逐根显示。
8. **取盘**：取走后灯灭（`CLEAR`，取盘检测用空位/定时），放下一盘循环。

### H3. 硬件↔软件 接口清单
| 接口 | 硬件侧 | 软件侧 | 状态 |
|---|---|---|---|
| 取图 | 双海康 GigE | `app/cameras/hik_camera.py`（枚举/取流/抓帧）；待加**第二台绑定** | 🔄 |
| 来料 | 相机写文件夹 | `FolderWatchFeeder` 监听 `WATCH_DIR` 自动识别 | ✅ |
| 触发 | 无（纯软件） | **静止即拍**：motion_trigger.py 盯预览帧，静止即触发 | 🔄 待写 |
| SN | 无（照片解码） | 标签二维码解码 `barcode.read_label_code` 优先、`read_label_vl` 兜底 | ✅ |
| 指示灯 | ESP32+WS2812 串口 | 拟新增 `app/indicator.py` + 接入 `analyze_and_save` 末尾 | ⛔ |
| 配置 | — | `.env`：相机SN绑定、`WATCH_DIR`、`INDICATOR_PORT/ENABLED/SLOTS` | 部分 |

### H4. 指示灯协议（PC → 灯板，一行一事件）
```
BUSY             # 识别中 → 全黄
1G 2R 3G 4-      # 出结果：槽i 状态 G绿(合格)/R红(不合格)/-灭(空位)/Y黄
CLEAR            # 取盘/复位 → 全灭
```
灯板固件（拟 `tools/indicator_esp32.ino`）：读串口一行 → 设置对应 WS2812 灯珠颜色。软件侧无板/断开时**空转不影响识别入库**。

### H5. 几个必须处理的衔接坑（硬件到软件）
- **双相机正反镜像**：反面相机拍到的左右是**镜像**的，正面第1槽 ↔ 反面对应槽要做**镜像映射**，否则同一根的正反面配错、拆记录张冠李戴。
- **放盘防呆**：托盘做不对称卡口或图上加基准标记，防止放反/转 180° 导致槽位全错。
- **图像质量门禁**：拍糊/过曝当场判"重拍"，不硬识别出错值。
- **两台同步 + 相机独占**：一次触发两台同时拍；海康一台只能被一个程序打开，两台各自管好句柄。
- **SN 可信**：追溯主键由**标签二维码解码**精确拿到（无需扫码枪）；大模型仅兜底。

### 7.5 托盘空位检测：先数数量，再识别（2026-07-14 已实现）
- 托盘 4 槽**最后一盘常不满**→有空位。要求：**先判每个槽有没有条(识别数量)，只对有条的槽识别与判定**；空位不当成不合格、不算"看不清盲点"。
- 做法：整图固定模板每面加**槽位层** `slots:[[x0,y0,x1,y1],…]`（归一化，左→右，长度=托盘容量）。识别前对每个槽算**边缘/纹理密度**（`detect_occupied_slots`：空槽=光滑塑料→边缘极少；有条=密集芯片+激光小字→边缘多），低于阈值 `SLOT_PRESENCE_MIN` 判空位。
- **空槽整槽跳过**（不识别、不进大模型、不进判定），标注图上画灰框**「空位」**（透明、不静默漏掉）；返回体带每面 `slots`（占用/分数）与本盘 `stick_count`（实际根数）。每颗结果带 `slot` 序号（为"一盘拆 N 条记录"打底）。
- 模板没写 `slots` 时按框 x 跨度自动聚类兜底（双列/紧排易误切，**真机模板务必写显式 slots**）。阈值用真机满盘/空盘各拍一张，跑 `tools/calibrate_slots.py` 标定。
- 实测（demo4 3根模板）：满盘→3 槽全占用、60 颗、pass；右槽涂平模拟空位→槽2 空、跳过、标「空位」，仅识别 40 颗、不误判。

### 7.6 一盘拆成 N 条独立记录（2026-07-14 已实现）
- 需求：**一根内存条 = 数据库里一条记录**。一盘 4 根拍一张图，识别后按槽位**拆成 N 条**入库，不再整盘挤成一条。
- 每根**独立**：①**逐颗判定只在本根内比较**（跨根不比——否则两根不同批次会互相超差、被误判不合格）；②各自**读自己的 SN**（把该槽区域裁出单独送大模型读标签）；③各自**外观质检**（该槽裁图单独送 VL）；④各自 PCB/主控日期、合格/不合格、不合格定位；⑤各自**归入当前批次**并继承登记信息（只 SN 每根不同）。
- 记录新增 `slot_pos` 列（托盘第几槽 1..N，左→右）；管理看板每条记录显示「托盘第N槽」标签。整盘原图/标注图 N 条共用（可追溯）。
- 触发路径：**自动监听/工位屏**（`analyze_and_save`）走拆分；工位屏一盘刷出**每根一行**合格/不合格 + 本盘 N 条已入库。检测到 **≥2 根**才拆；单根/规则模式仍整图一条（旧行为不变）。
- 实测（demo4 3根模板）：一张图 → 3 条记录（第1槽 PCB2532、第2/3槽 2536，各20颗，各自 pass），`slot_pos`=1/2/3 正确入库。
- 性能：每根 2 次大模型调用（外观+SN），N 根共 2N 次，已用线程池并发（墙钟≈单根）。

### 7.7 本轮软件闭环（2026-07-16 已实现，纯软件、已验证）
- **标签二维码解码 SN（取代 OCR 猜 SN，精确）**：内存条标签上的**大 DataMatrix** 里编码了结构化信息 `(L)规格(S)序列号(P)型号(M)批次码`。识别时对每根正面裁图用 **zxing-cpp** 解码（`app/recognition/barcode.py`），**精确拿到 SN/型号/规格**（带纠错、零误差），忽略芯片上的小数字码。SN 来源改为：**①标签二维码解码（优先） → ②大模型读标签（兜底）**（`services._read_label`）；数据库 SN/型号/容量/频率**按解码结果保存**（有批次登记仍以登记为准）。4 根同一张照片时按槽位裁图逐根解码、各归各根。实测样例 `samples/region_front.png` 解出 `SN=80CE02253633436BCE / 型号 M321R8GA0EB2-CWMXH / 64GB / DDR5-5600`。直接把痛点 **E1「SN 靠大模型不可靠」** 解决，**无需扫码枪**（解不出才用大模型兜底）。
- **标签/白区框位跳过**：白纸标签随机贴、随机盖住若干颗芯片，固定框撞上标签会把标签 SN 数字误读成日期。新增 `region_ocr._box_kind`——框区**又白又平**(亮度≥`LABEL_WHITE_MIN`、边缘≤`LABEL_EDGE_MAX`)判为标签，`recognize_side` **整框跳过**（被盖芯片"不用管"，只求别误读）。demo4 无标签→60 颗不回归。
- **防重复放盘（幂等）**：`services.tray_fingerprint`（一盘 4 根 SN 去重排序做指纹）+ 本批(`batch_id`)已见集合；同批再放同一盘→`analyze_and_save` 返回 `duplicate:True` 不重复入库。有效 SN<2 不去重、新批次清零（隔天合法复检不受影响）。
- **手动识别页逐根显示**：`index.html` 多根托盘在综合判定下**每根一行**（第N槽·合格/不合格·颗粒数），与工位屏对齐。
- **报表导出**：`GET /api/records/export?format=xlsx|csv&客户/批次/判定/日期`（`db.list_records_filtered`）→ 下载对账/质检报告；manage 页「导出Excel/CSV」按钮。
- **按 SN 追溯 + 同 SN 历史比对（防偷换第二道）**：`GET /api/records/by_sn/{sn}`（`db.list_records_by_sn` + `services.sn_history_diff`）→ 该 SN 历次记录 + 若颗粒日期集合/主控/PCB 变了则 `changed` 告警；manage 页「输入SN查历史」。
- 单测：`test_dedup.py` / `test_sn_history.py` + `test_slot_occupancy.py` 增 `_box_kind`；全套 39 例绿。

### 7.1（已弃用）弹仓 / 电机传送 / 到位信号双向握手
> **2026-07-14 全部弃用**。原方案（3D打印弹仓 + 步进电机分度送料 + 光电到位 + Arduino/串口 READY/DONE 双向握手 + SimulatedFeeder/SerialFeeder）**不再使用**，改为**托盘固定位 + 每盘拍照**（见 §七）。相关代码已清理：`app/pipeline/feeder.py` 只保留 `FolderWatchFeeder`（目录监听），`SimulatedFeeder`/`SerialFeeder` 已删除。此条仅留档说明决策变更。

### 7.1.6 工位屏（内部自用工位流）✅
- 现场流程：**人只放一盘料到托盘 + 取出**；相机**拍正反两面、写到文件夹**；不用扫码枪，SN 由**标签二维码解码**（大模型兜底）。
- `web/station.html`（`GET /station`）：**全屏零点击**——选当前批次 → 开始监听目录 → 放料，系统自动识别+判定+存入当前批，屏上刷出**大号 合格(绿)/不合格(红) 灯 + SN + 不合格定位 + 正反标注图 + 本批已检/良品/良率**，提示「已存，请取出放下一盘」。
- 复用 `FolderWatchFeeder`(目录监听) + 批次绑定（`/api/pipeline/start` 带 `batch_id`）+ SSE 实时推送；托盘取景一致，配**整图固定模板**（一盘4根一次识别）。

### 7.1.4 已实现：监听相机输出文件夹，自动识别 ✅
- 相机拍完一盘/一份，会在输出目录下**自动生成一个子文件夹**，内含正反两张照片。
- `FolderWatchFeeder` 持续盯着该目录：出现**新子文件夹**且**两张都写完**（大小连续稳定 + 可解码）→ **自动识别+质检+归批入库**，落 `.done` 标记（重启不重复），继续等下一份。
- 目录由 `.env` 的 `WATCH_DIR` 指定（默认 `test_photos/`，生产改成相机实际输出目录）。
- 接口：`POST /api/pipeline/start`(operator, mode, template_id, batch_id…) / `/stop` / `GET /status` / `GET /stream`(SSE)。**已无 source=sim/弹仓相关参数**。

### 7.1.7 已接入：双相机（上/下）按SN绑定 + 同时抓正反 ✅（2026-07-17）
- 两台海康 MV-CS050-10GC **上下对置**，按**相机序列号**绑定角色(不靠插口)：**上=正面 SN DB1224590、下=反面 SN DB1623157**（`.env` 的 `HIK_FRONT_SN`/`HIK_BACK_SN` 可改）。
- `hik_camera` 改为**角色注册表**：每台一个独立句柄，`get_camera('front'/'back')`；`capture_both()` **两线程并行同时抓**上下两台各一张(防正反错位)。实测 0.83s 出两张 2448×2048 图。
- 接口带 `side=front|back`：`/api/hik/preview`、`/api/hik/capture`、`/api/hik/exposure`；新增 **`POST /api/hik/capture_both`** 一次出正反两张(托盘工作流)。
- **一键链路已打通**：`POST /api/hik/capture_and_save`（内部 `capture_both` → `analyze_and_save`）= **同时拍正反 → 识别 → 拆 N 条入库**；工作台加了**上/下双预览** + **「同时拍正反 → 识别入库」按钮**，现场"放盘→点一下"即完成。真机跑通(读出真条 SN 并入库)。
- **静止即拍（自动触发，2026-07-17 已写）**：`app/pipeline/motion_trigger.py` 盯上相机预览帧，帧差状态机"放盘→画面静止(连续N帧)→自动拍→已拍锁定→取盘运动→重新武装"，触发即走 `capture_and_save`，结果推现有 SSE。接口 `/api/hik/auto/start|stop|status`；阈值 `.env` 可调（AUTO_MOTION_THR/STILL_THR/STILL_FRAMES/COOLDOWN）。**手动拍照全部保留**，两条通道并存。实测：启动不误触发、可停；触发路径复用已验证的一键链路。待现场标定阈值 + 真机验证触发。
- 每台仍是独占——用前先在 MVS 客户端断开；GigE 快照式无带宽压力。

### 7.1.5 已接入：海康工业相机实时画面 + 抓拍 ✅
- 现场相机 = **海康 MV-CS050-10GC（GigE）**，SDK 为海康 MVS（装在 `E:\MVS`）。单台，正/反面翻面分两次拍。
- 新增 `app/cameras/hik_camera.py` 封装 MVS Python SDK：枚举/打开/连续取流/抓帧存 JPG/转 BGR/`mjpeg` 实时流。
- 接口 `GET /api/hik/status`、`GET /api/hik/preview`（MJPEG）、`POST /api/hik/capture?side=front|back`。
- 前端「实时画面」卡：相机状态 + ▶打开/■关闭实时画面 + 📸拍正/反面 + **曝光时间输入框**（µs，实时应用）。实测：枚举到相机、预览出帧、抓拍成功、曝光可设（15~9.96e6 µs）。
- **曝光/增益代码可调**：`hik_camera.set_exposure(us)`/`set_gain(db)`；`.env` 配 `HIK_EXPOSURE_US`/`HIK_GAIN` 开机应用；接口 `GET/POST /api/hik/exposure`。清晰度不够时先调曝光+打光。
- **占用约束**：相机同一时刻只能被一个程序独占；用本系统时先在**海康 MVS 客户端断开相机/关闭 MVS**（否则 `0x80000203`）。

### 7.2 相机/设备选型（评估中）
- 候选：①**边缘板(树莓派/Jetson)+高分辨率相机模组**（脚本可编程，最灵活）②**工业相机(海康/Basler/大恒)+硬件触发**（全局快门、到位触发、工业稳）③**智能相机(Cognex/Keyence)**（相机内跑逻辑，贵）。排除 ESP32-CAM/OpenMV（分辨率不够，拍不清小字）。
- "编程"的落地：树莓派/Jetson=Linux 上写 Python(picamera2/OpenCV)；工业相机=厂家 SDK 配触发+取图；智能相机=自带脚本环境。

> 待你确认：①相机→服务器走 **A(HTTP 推送)** 还是 **B(文件夹落盘)**？②相机端能否带 **SN/批次号**？③固定**正+背两张**，还是还要 PCB/主控特写？④选型倾向（边缘板/工业相机/智能相机）+ 预算 + 是否已有 PLC/到位传感器？

## 【历史决策】（已被上面替代，留档）

- 曾计划用服务器端 OpenCV 直接管 UVC 4K 相机 + 手动/自动"放稳即拍"。现改为**相机端自带主控芯片、脚本内自动检测触发**，服务器只接收处理。相关的 `app/cameras/camera.py`（OpenCV 本机采集）保留作**测试**用途。

## 7.4 逐颗置信度 + 低置信大模型兜底（已实现）
- PaddleOCR 逐颗给**真实置信度**（取该颗读数所在检测的 score）。
- **置信度 < 阈值 或 OCR 未解码** 的颗粒 → **逐颗交给大模型识别**（每颗单图单调用 `read_crop_vl`，用线程池并发不变慢；比批量拼多图更准、不会相互串位/干扰）；发送前统一翻正+增强(`_prep_for_vl`)。高置信的仍走 OCR。阈值 `OCR_CONF_MIN`（默认 0.85，前端明细显示每颗**真实** OCR 置信度）。**透明**：大模型读出后**不覆盖** `ocr_raw`/`ocr_confidence`，note 摊开「OCR原文(置信) → 大模型读为X」。
- 实测（模板模式，demo2 一根）：逐颗兜底 + 透明输出后，正反面全部颗粒都给出读数（真实置信照显）；这张照片质量差，仍有个别颗粒被 OCR/大模型误读成离群值 → 按 §四**逐颗判定**会判不合格并定位到那颗（这是可接受的误报，靠人工复查兜底；根治靠提高拍摄质量）。整根正反两面约 5~6s（稳态）。

#### 7.4.2 整图大模型核对漏检（开关，已实现）
- OCR/模板是**逐框**读，某颗没框到就**静默漏检**；开「整图大模型核对漏检」后，识别完对正/背面**用大模型整图数一遍颗粒总数**（`count_dram_vl`），与 OCR 检出数比较。
- **大模型看到的比 OCR 多 ≥2 颗** → 前端红黄条提示「⚠ 疑似漏检 N 颗，请人工核对」，并把 `coverage{detected,total,missing,dist}` 附在该面结果里。
- 分工：**OCR/模板给逐颗位置+判定（防偷换、定位第N颗）**，**大模型整图保覆盖（防漏读）**，互补。
- 开关：前端「高级参数」勾选；接口 `vl_check=true`（`/api/recognize`、`/api/folder/recognize`、`/api/hik/recognize` 都支持）。默认关（多花一次大模型调用）。
- 实测 demo6 全分辨率：OCR 检出 19、大模型看到 16 → 不报警（OCR 没漏）。

#### 7.4.1 多方案提准（已实现，专治"肉眼清楚模型却读错"）
针对"字倒装 / 低对比 / 点阵字被读成形近字母"三类常见误读，逐颗做多方案识别（`_best_read`）：
1. **自动判方向 + 翻正**：DIMM 下半区芯片是 180° 倒装的，人眼自动转正、OCR 不会。逐颗试原样与翻转 180°，取文字置信更高的朝向为"正"再读（实测倒装颗粒 `0h903S`0.66 → 翻正 `SEC540`0.96）。
2. **对比增强(CLAHE)**：对正确朝向做局部对比增强，救回发灰/低对比的激光小字。
3. **字符混淆挽救**：SEC/SAMSUNG 锚点后的 3 位后缀里把形近字母映射回数字（`S→5 O→0 I→1 B→8 …`），`SECS40→540`，OCR 直接读对、不必走大模型。
4. **大模型兜底喂"增强+翻正"图**：仍读不出的才交大模型，且给的是方向/对比都处理好的图（而非原图），命中率更高。
- 效果（demo2 这张较差的照片）：正面 15/15、背面 20/20 全部读出，误读从 ~7 颗降到 ~3 颗；剩余 2~3 颗是 OCR 读空/读残的真·degraded 颗粒（如 #20 四种变体 OCR 全空、#19 SEC540 被读成 038），软件已到极限，须靠**拍摄质量**根治。稳态耗时约 5~6s（首次冷启动含模型加载会更久）。
- SN/品牌/型号/频率 **全部由大模型读标签**（`read_label_vl`）。
- **原始透明输出（取代"遮挡"，本轮改定）**：不再把读不出的颗粒标成「遮挡」。要求**看每颗的原始真相**：
  - **保留 PaddleOCR 原始读数与真实置信度**（`ocr_raw`/`ocr_confidence`），**不被大模型改写抹平**（以前兜底后一律显示 0.85，掩盖了真实的 0.66）。
  - **低置信/未解码仍发大模型兜底**（保准确率），但显示**透明**：界面同时给出「OCR原文(真实置信) → 大模型读为X」，`大模型`徽标标出哪几颗是兜底读的。
  - **错的也照显、不粉饰**：大模型/OCR 读错了就显示那个错值（如 25年34周），读残就显示 OCR 原文（如 `SEC5`/`038`），完全无读数才「未识别」。**任何情况都不显示「遮挡」**。
  - 判定端：真正 OCR+大模型都读不出（无年/周）的颗粒是**盲点**，按 §四单列「请人工确认」（防被换芯片藏在读不出的颗粒里），但不再用"遮挡"字样、也不预标位置。

## 7.3 性能优化（已实现）
- **大模型三路并行**：识别(OCR+PCB大模型) / 外观质检 / 读标签 用线程池并发（`_analyze_all`），墙钟≈最慢一路。相机/文件夹/流水线均用。
- **默认更快模型**：`QWEN_VL_MODEL=qwen3-vl-plus`（比 235B 旗舰快很多）。实测一整轮 ~7s（并行前 ~15–20s）。
- 再提速：产线用模板模式(OCR 亚秒)、标签二维码解码代替读标签、必要时换更轻的 `qwen-vl-plus`。

## 八、工程化缺口评估 & 待办（作为产线项目还差什么）

> 核心识别链路已通（相机→识别→判定→入库→看板）；离"生产级"还差以下几块，按**重要性**排。

### 🔴 一档：不补上产线会出事

**1. 进程守护 & 运维（最欠缺）**
- 现在 `nohup uvicorn` 手动起——崩了不自动重启、关机不自启（已多次撞上端口被孤儿进程占死）。需 **Windows 服务（NSSM）/ 计划任务**托管：崩溃自拉起、开机自启。
- **无日志系统**：报错进 stdout 就没了。需**分级日志 + 按天滚动 + 错误留档**。
- **无监控告警**：相机掉线 / 大模型超时·欠费 / DB 挂 / 良率骤降 目前无感知。需状态面板 + 关键异常告警。

**2. 数据追溯 & 数据库（质检命根子）**
- **质检图片没跟记录绑定归档**：`uploads/` 手动删、`outputs/` 临时。追溯/纠纷举证要求每条记录**永久留存当时标注图**并可回看——当前断了。
- **建表迁移是"删表重建"**（`db.py` 检测旧结构直接 DROP）：改表结构=历史数据全没。需**增量迁移**（加列不删数据）。
- **无备份策略**：MySQL 单点、无定时备份。
- **无审计**：谁改了"复查合格"、何时改的，查不到。

**3. 权限 / 安全（现在是裸奔）**
- ~~**零鉴权**：同局域网任何人可改记录/删模板/删操作人/启停流水线~~。✅ 已解决(Phase 5):登录 + 注册 + 管理员审核,角色 admin/operator,中间件统一门禁。
- 纯 HTTP、record 保存收任意 JSON——内网可接受，正规交付要收口。

**4. 自动化测试 & 评测集（现在全靠手测）**
- **零测试**。`date_parser`、`services.compute_signal`（判定逻辑）是纯逻辑又最关键，改一行可能悄悄改错判定。应**优先补单元测试**。
- **无带标准答案的评测集**：准确率靠感觉。攒 50~100 根标注样片做**回归评测**，量化准确率、防退化。

### 🟠 二档：产品价值 & 现场落地
- **批次登记（Phase 4 前置，本轮明确）**：每批测试前先登记 `客户 / 品牌 / 容量 / 频率 / 品相(拆机·拆新·全新) / 备注(来源，如香港货·马来货) / 批次号(单号)`；登记后这批每根**自动归入并带上这些信息**。**登记为准**——品牌/容量/频率入库直接带，**每根不再读标签，只读 SN**（标签二维码解码，大模型兜底）。数据：新增 `batches` 表（批次号/客户/品牌/容量/频率/时间/在检）；`inspection_records` 增 `customer/capacity/batch_no` 列，入库时从"当前批次"继承。
- **良品率看板 / 质检管理页**：按 批次/客户/今日/累计 统计良率（verdict=pass 为良品），独立页 `/manage`。**已有方案**（见 `.claude/plans/`），未实现。
- **托盘拍照触发接入**：改用托盘（弃用弹仓/电机/握手，见 §七）。相机写文件夹→`FolderWatchFeeder` 自动识别已具备；待定"正反面怎么拍 + 触发方式(按钮/到位开关/定时)"。
- **不合格品处置流程**：判 fail 之后的返工/隔离/贴标闭环，没有。
- **报表导出 / 打印**：Excel/PDF 导出、质检报告或标签打印，没有。

### 系统设置：多模态大模型多 provider（本轮）✅
- `web/settings.html`（`GET /settings`）：**大模型配置中心**——服务商列表(可接多个模型)、当前启用哪个、模型名/base_url/APIKey(打码)/超时/单价，可新增/编辑/切换/删除；**用量统计**（调用次数/token，填单价则估算费用）。
- `app/settings_store.py`：多 provider JSON 配置（首次从 .env 播种）；`quality_inspect` 改从"当前启用 provider"取 base_url/model/key/timeout（回退 .env）。只要 OpenAI 兼容接口（DashScope/GLM-4V/OpenAI/本地 vLLM）加一条即可切换——**为接入多个多模态模型预留位置**。
- 用量记入 `app/metrics.py`（VL 调用次数+token），`GET /api/settings/vl` 返回；重启清零，余额需服务商后台查。
- **降级容错（本轮）**：所有大模型调用收拢到 `quality_inspect._chat()`，按"当前启用排第一、其余跟随"(`settings_store.ordered_providers()`)**逐个尝试**——第一个网络/欠费/超时/报错就**自动切下一个**，全失败才报错；每次降级写日志。即便现在只配一个也无副作用；配了第二个就自动兜底。实测：坏模型排第一 → 自动降级到真模型 → 成功。

### 一图多根识别（实验，本轮新增，不改原逻辑）
- 需求：一张照片拍 3~6 根（一面），另一相机拍另一面，一起识别。
- 新增 `app/routers/multi.py`（`POST /api/multi/recognize`）+ `web/multi.html`（`GET /multi`）：一张（或正+背两张）多根图 → 按 行/列**等分**成 N 根 → 每根走**现有** `services.run_recognize` → 返回 N 个结果（各自判定/颗粒/标注图）；正背两图按第 i 块配对。**全部新增，未改动原有识别代码。**
- v1 用"等分切"（适合摆满画面、均匀排布的固定治具）；真机固定后可换"多槽模板"精确切分。分辨率是关键——每根要留够像素（现相机建议 ≤4 根、摆满画面、好打光）。

### 🟡 三档：识别能力本身（决定好不好用）
- **拍摄质量 / 打光（第一杠杆）**：软件已到天花板，剩余准确率靠**打光 + 对焦 + 固定治具**（硬件，现场推进）。
- **模板只有 1 个**：客户几十种型号，规则模式有噪声漏检。需**批量建模板工具**（自动框）+ 覆盖在检型号。
- **大模型是外部依赖**：DashScope 网络/费用/限流/延迟/可用性——无**降级、缓存、成本统计**，断网即瘫。

### 🟢 加分：工程化
- **Docker / 部署 runbook**（GPU+相机 SDK 打包难，至少一键部署脚本 + 新机上手文档）。
- 前端 `index.html` 渐大，迟早拆组件 / 加构建。

### Phase 1 已完成 ✅（进程守护 + 日志 + 健康监控）
- **日志** `app/logging_setup.py`：分级 + 按天滚动 `logs/app.log`（留 14 天）；server 请求日志中间件（方法/路径/状态/耗时，静态与高频轮询降噪）；DB/启动改 logger；WARNING+ 自动计入错误数。
- **指标** `app/metrics.py`：内存计数（处理数/pass/fail/错误/uptime/良率），`db.save_record` 每存一条自增；`GET /api/metrics`。
- **健康** `GET /api/health` 增 `db_ok / hik_cameras / dashscope_key / version`。
- **进程守护** `scripts/run_server.bat`（:loop 崩溃自重启）+ `scripts/service_install.ps1`（任务计划开机自启，零依赖；管理员运行；`-Uninstall` 卸载）。
- 实测：health/metrics 正常、日志滚动、存记录 metrics 自增并留日志。

### Phase 2 已完成 ✅（数据追溯：增量迁移 + 图片归档 + 审计 + 备份）
- **增量迁移**：`db.init_db` 改用 `_ensure_columns`（查 information_schema→缺列 ALTER 补），**不再 DROP、改表不丢数据**；实测老库 6 条记录原样保留、自动补上 4 个图片列。
- **采集原图有序归档**：双相机每次抓拍的正反两张统一命名 `front.jpg`/`back.jpg`，存进新建的**有序子文件夹** `uploads/<序号>/`（序号 0001、0002… 递增，代表拍摄先后）；`uploads/` 根目录只含有序子文件夹、不再堆散图。`analyze_and_save` 对已在 uploads 下的采集图**就地识别、不再复制成扁平 `{uid}_{slot}.jpg`**（`_recognize_core` 的图片 URL 改用相对 uploads 的路径含子文件夹）。
- **图片归档追溯**：新增永久区 `archive/<日期>/<uid>/`（`server` 挂 `/archive`）；`services.archive_record_images` 把当次原图+标注图从 uploads/outputs **复制进归档**并把记录 4 字段（front_img/back_img/annotated_front/annotated_back）改写成 `/archive` URL；`analyze_and_save`（自动流水线）与手动 `POST /api/records` 都归档 → uploads 被清也能追溯。
- **审计**：新增 `audit_log` 表 + `db.add_audit/list_audit`；改复查、删模板/操作人写审计；`GET /api/audit` 查看。
- **备份**：`scripts/backup_db.bat`（mysqldump→`backups/`，保留 30 天）+ 任务计划每日。
- 实测：迁移不丢数据、归档 4 图落盘且记录带 /archive 路径、复查改动写入并可查审计。

### Phase 3 已完成 ✅（单元测试 + 评测集）
- **单元测试**（`tests/`，pytest，**39 例**全绿 0.4s；新增 `test_dedup.py` 防重复放盘、`test_sn_history.py` 同SN比对、`test_slot_occupancy.py` 增标签/白区判别）：`test_date_parser.py`（YWW/YYWW 解码、世纪规则、非法周、YYYYWW 归一）；`test_compute_signal.py`（同批 pass / 阈值边界 / **单颗被换必判 fail 并定位到第N颗** / 盲点单列 / 有效<2 unknown）；`test_slot_occupancy.py`（**托盘空位检测**：边缘密度判空/满、显式slots/自动聚类、框归槽）；`test_stick_split.py`（**逐根判定**：跨根不比较·不同批次两根各自合格、根内被换芯片只该根fail并定位、盲点按根隔离）——锁住判定链与拆分正确性。
- **评测脚本** `tools/eval_accuracy.py`：给定"样本目录 + 每根 `truth.json`(expected_yyyyww)"→ 复用 `services.run_recognize` 跑识别 → 输出每颗读对率、错在哪几颗、整条判定是否符合预期。实测 demo2(模板模式)：**85.7% (30/35)**、判定符合预期 1/1。
- 依赖：`pyproject.toml` 加 `[dependency-groups] dev=[pytest]`；根 `conftest.py` 保证 `import app`。

### Phase 4 已完成 ✅（批次登记 + 良品率看板；登录鉴权暂缓）
- **批次登记**：`batches` 表 + `POST/GET /api/batches`；前端左栏「当前批次」卡（登记 客户/品牌/容量/频率/批次号 或选已登记批次）。每根质检**归入当前批并继承登记信息，只读 SN**（实测继承正确）。
- **良品率看板**：独立页 `/manage`（`web/manage.html`）——今日/累计良率 KPI、**按批次**（含已检/良品/良率）、**按客户**、记录明细（判定/说明/归档图链接/复查下拉）。统计接口 `/api/stats/overview`、`/api/stats/customers`、`/api/batches`；`verdict='pass'` 为良品。顶栏「质检管理」改为跳转 `/manage`。
- **登录鉴权**：见下 Phase 5（已实现）。

### Phase 5 已完成 ✅（登录 + 注册 + 管理员审核，受控准入）
- **入口页 `/login`**：Apple 风极简页——中央动态**橙↔蓝圆点环**(取色自 logo)+ 登录/注册切换；注册填 用户名(姓名)/手机号/密码(去掉分公司·部门)。新人自助注册 → 状态 `pending`。
- **管理员审核 `/users`**：管理员审核通过(approve)/拒绝(reject)/停用/重置密码；只有 `approved` 才能登录。
- **角色 admin / operator**：质检员只能用工作台 `/`；`/manage`·`/settings`·`/users` 及 `/api/users*` 仅管理员([app/server.py](app/server.py) 中间件统一门禁,未登录页面跳 `/login`、接口 401)。
- **实现**:密码 `pbkdf2_hmac` 加盐哈希(不存明文);会话 token 存 `sessions` 表 + HttpOnly cookie `sid`(同源自动带,**现有前端 fetch 零改动**);种子超管由 `.env` `ADMIN_USER`/`ADMIN_PASSWORD` 首启自动建。**零新增第三方依赖**(标准库 hashlib/secrets)。新表 `users`/`sessions`。
- 实测:注册→待审核不能登录→审核通过后能登录;质检员无管理员权限;密码哈希可校验、错密码拒绝;`pytest` 全绿(+`tests/test_auth.py`)。

### 建议推进顺序
1. **进程守护 + 日志**（任务计划托管 + 分级滚动日志）——投入小、当天见效、最止血。✅
2. **图片归档 + 记录绑定 + 增量迁移**——保住追溯、杜绝丢数据。✅
3. **给 `date_parser` / `compute_signal` 补单元测试 + 评测脚本**——锁住判定正确性。✅
4. 再做**良品率看板**（已有方案）✅ 与**登录鉴权**（暂缓）。
5. 现场同步推进**打光/治具**（产品能用的前提）。

---

## Phase 5 已完成 ✅（双相机 + 静止即拍 + 二维码全字段，2026-07-17/18）
- **双相机接入**：海康 MV-CS050-10GC ×2，按 **SN 绑定** 上=正面(DB1224590)/下=反面(DB1623157)，不认插口；`capture_both` 两线程同时抓正反（~0.83s）；一键「拍正反→识别→拆N条入库」真机跑通。
- **正反方向对应**：反面相对正面是左右镜像 → 采集时 `fliph` 翻正（`.env` 持久化），故 **正 i ↔ 反 i identity 映射**，拆根按同槽号取正/反已对齐（真盘 4 根待肉眼终验）。`/api/hik/orient` 可切 none/fliph/flipv/rot180。
- **曝光/增益**：上下各自可调并 **持久化到 `.env` 开机自动应用**（防相机断电回默认变黑）；支持读回 MVS 调好的值固化。
- **★静止即拍★**：帧差状态机（待机→等静止→拍→锁定→取盘重新武装），**真机端到端实测通过**（放盘→自动拍→识别→解SN→判定→入库）；阈值现场标定（运动2.5/静止1.5，实测噪声0.74/放盘峰值~10）；**连续作业**（放→拍→取走→放新盘）已实测循环、每盘框图/记录刷新。
- **二维码全字段**：`(S)`SN/`(P)`型号/`(L)`规格→抽频率容量、**品牌由型号前缀推断**；SN/品牌/型号/频率/容量/**规格spec/批次码mfg** 全字段入库（增量加列不丢史）。
- **稳定性**：GigE 限帧5fps+加大缓冲、抓拍掉线自愈、运动循环卡流自愈、**网卡<1Gbps 前端红字告警**（曾掉到10Mbps致取帧超时）、预览掉线自动重连、SSE 断线自动重连。
- **前端**：静止即拍实时流程条（状态机+帧差进度条+已拍数）、**结果内嵌标注框图**（每盘刷新不留旧图）、每台曝光/增益/翻转、关闭/释放相机（供 MVS 接管调参）。
- **现场发现**：① 字迹不清的条用**特制清洁液擦拭**→丝印/日期码极清晰（可纳入上料前工序）；② **单根**内存已找到好**打光角度**（压反光/救淡字）+ **遮光布**配合成像不错——待推广到整盘 4 根同时。

## 当前缺口与待办（截至 2026-07-20，按优先级）
> 结论：软件"管道"已很完整；从 demo 到产线，关键在**识别准不准（真机基准）**与**出错能否兜住（健壮性）**。

**🔴 决定能否上线（核心）**
1. **识别准确率『真机基准』**：攒真实产线条（不同品牌/DDR4-5/脏/氧化/淡字）贴标注 → 实测准确率，尤其**日期码读错率**。现只测过干净样品，这个不做别的都是空中楼阁。
2. **整盘 4 根全验证**：真机 4 槽模板（正/反）、**4 根同时打光**、4 根拆条、正反槽位映射、**端到端节拍**（rules 17s 太慢，template 4 根几秒/盘、跟不跟得上人放盘）。

**🟠 无人值守健壮性（易被忽略）**
3. **图像质量门**：拍糊/过曝/欠曝 自动重拍或标待人工（静止即拍最大隐患：手未离/抖动拍到糊图会静默存错）。
4. **应检数硬校验**：每型号定应检数（正:主控1+存储N / 反:PCB1+存储N），**实读<应检数→该根判「待人工」，绝不当合格放过**；无模板时用大模型芯片数当参照。规则识别先天漏检的安全兜底。
5. **异常兜底 + 防呆**：相机中途掉线/DB异常/SN解不出（**大模型猜的要标红**别当精确值）/多放少放——统一优雅降级，异常明确标红不静默。
6. **待人工复核队列**：`review_status` + SN历史比对已有，缺「待复核」集中列表 + 一键复核 UI，让盲点/unknown 有明确去处。
7. **取景漂移自检**：治具被碰→框位偏→静默裁错，无人值守必须自检告警重标定。

**🟡 闭环 / 集成 / 运维**
8. **指示灯灭灯闭环**（硬件在途，到货接上即用）。
9. **批次质检报告/证书**（合格率+逐根清单+SN追溯+图片，给客户对账举证）。
10. **金蝶 ERP**（采购订单带出批次，卡平台+凭据）。
11. **故障主动告警/监控**（相机掉/识别异常/磁盘满 主动推送；无人值守必备）。
12. **部署 runbook / Docker**、**登录鉴权**（暂缓）。

**下一步建议**：先做 3+4（纯软件、当天能落地，直接堵住"漏了还判合格"这个最严重漏判）→ 再 1+2（真机基准 + 4 根整盘打光/模板/节拍）→ 成像上游（打光/清洁/遮光托住召回上限）→ 硬件到货补 8 → 外部就绪补 9/10/11。
